# AGENTS.md

## プロジェクト概要

このリポジトリは、海城中学高等学校 情報科の教材サイトである。

- 公開方式はGitHub Pagesによる静的サイト。
- 主な教材はHTML/CSS、Illustrator、スプレッドシート、Python。
- 主な利用環境は生徒用MacBook Air、SafariまたはGoogle Chrome。
- 教材内容の正確さだけでなく、授業時間、学習段階、試験範囲、配付物との整合性を重視する。
- ユーザーとのやり取りと作業報告は原則として日本語で行う。

## 作業開始時

作業前に必ず次を確認する。

```sh
git status --short --branch
git diff
git log -1 --oneline --decorate
```

- 既存の未コミット差分をユーザーの作業として扱い、失わない。
- 関係のない変更を修正、整形、コミットしない。
- `git reset --hard`、`git checkout --`などの破壊的操作は禁止。
- 現在のブランチと`origin`との差を確認してからコミット、pushする。
- 大規模な機械置換より、対象を限定した小さな差分を優先する。

## Git運用

- コミット、pushは、現在の依頼または明示された継続的な許可がある場合に限る。
- 過去の特定スレッドだけで与えられたコミット、push許可を、恒久的な許可と解釈しない。
- コミットには今回の作業ファイルだけを含める。
- push前に`origin`の更新を確認し、force pushは行わない。
- PR作成やmainへのマージは、明示的に依頼された場合だけ行う。
- 作業後は変更ファイル、実装内容、検証結果、未確認事項、コミットSHA、push先を報告する。

## 原本と生成物

### `js/pages.js`

`js/pages.js`はページ一覧Googleスプレッドシートを原本として生成される。

ページ一覧シート:

https://docs.google.com/spreadsheets/d/1d4--Rcap1BmZJPWJoEuK9teqoGn6Jyjzn_Q1FcDTvqE/edit?gid=70227298#gid=70227298

- ページの追加、名称変更、公開状態、前後関係、配付物リンクは、可能な限りページ一覧シートを先に更新する。
- `pages.js`から生成されるリンクを、各HTMLへ重複してハードコードしない。
- `release: true`を公開判定の基準として扱う。
- `pages.js`を直接変更する場合は、次回生成時に上書きされる可能性を考慮する。

### スライド由来教材

- `data/slides/<ID>.json`と`img_slide/*.png`は生成物であり、原則として直接編集しない。
- `ss11.html`〜`ss22.html`の全文型ページは直接編集せず、`config/slide-pages.json`、`templates/slide-page.html`、Googleスライド側の原本を変更して再生成する。
- 発表者ノートは公開文章として扱い、教員専用メモを混在させない。
- ハイブリッドページでは概念説明をHTML、操作手順をスライドJSONに置く。
- `data-slide-section`はスライド側のセクション名と完全一致させる。
- 詳細な公開手順は`docs/slide-html-publishing.md`も確認する。

生成と検証:

```sh
node scripts/generate-slide-pages.mjs
node scripts/generate-slide-pages.mjs --check
node scripts/validate-slide-content.mjs
```

### Colabノートブック

Colab原本は別プロジェクトの`~/Documents/Colab/`に同期されている。

- 教材サイトとノートブックで説明、表記、コードを変更する場合は原本との整合性を確認する。
- サイト上の表示だけを直して原本を放置しない。
- 別リポジトリへの変更、pushは、その作業範囲が明示されている場合だけ行う。

### HTML配付ファイル

- HTML実習ページを変更した場合、対応する`html/`内の配付用ファイルも確認する。
- 課題の意図に関係する修正は、教材ページと配付ファイルで揃える。
- サンプル、解答ファイルは公開教材ページとは別物として扱い、依頼なく一括修正しない。
- `sample2-1.html`は配付対象にしない。

## JavaScript構成

共通処理は`js/main.js`から段階的に初期化される。概ね次の順序を保つ。

1. `head.js`、`pages.js`、`faq.js`
2. DOM準備
3. `script.js`による共通レイアウト
4. `nav.js`、`highlight.js`、検索コア
5. 検索UI、`lesson_dock.js`
6. `script_pages.js`
7. 動的コンテンツを含む最終ハイライト処理

- 任意機能の失敗でサイト全体を停止させない。
- 同じ処理を複数ファイルへ重複実装しない。
- 動的に生成される内容を考慮し、初期化順序を変更するときは影響範囲を確認する。
- グローバル関数を増やす場合は必要最小限にする。

### `highlight.js`

- Python、HTML、CSS、表計算、実行結果などのコード表示を処理する。
- 入力内容を必ずエスケープし、教材コードを任意HTMLとして実行させない。
- 文字列、コメント、HTML属性、CSSリテラルなどが後続の正規表現で再処理されないよう注意する。
- `pre code`の構造がページごとに違う場合でも壊れないようにする。
- 見出し番号はすべての`h2`へ無条件に付与しない。
- 番号を付けない見出しには`data-skip-numbering`を使用する。
- 「確認問題」「次回は…」や目次の「はじめに」は本文の章番号へ含めない。

## LessonDockとナビゲーション

- LessonDockの既存機能を壊さず、追加機能の失敗を他のボタンへ波及させない。
- 「次回」アイコンは遷移先が1ページだけの場合に限り、クリックで直接移動する。
- 複数の次回ページがある場合は、アイコンクリックだけで勝手に遷移させない。
- パネル内にフォーカスがある間は、`mouseleave`の遅延処理で閉じない。
- 左目次、LessonDockパネル、検索dialogなどは`joho:overlay-open`によって排他的に開く。
- トップページでは左目次を開かない。
- 左目次の4pxの細いハンドルは意図的なデザインなので、太くしない。

## サイト内検索

- 検索はブラウザ内で完結させ、外部検索サービスへ検索語を送らない。
- `data/search-index.json`は生成物なので直接編集しない。
- `js/pages.js`、公開HTML、`data/slides/*.json`を変更したら検索索引を再生成する。
- 詳細は`docs/site-search.md`も確認する。

```sh
node scripts/generate-search-index.mjs
node scripts/generate-search-index.mjs --check
node scripts/test-site-search.mjs
```

- 検索対象は`pages.js`の`release: true`ページのみ。
- FAQ、中継ページ、テスト、未公開ページ、配付用サンプル、解答は索引へ含めない。
- 検索結果は文字列結合による`innerHTML`へ渡さず、テキストノードとして構築する。
- FAQ検索処理やFAQbot URL生成をLessonDockやサイト検索側へ重複実装しない。
- FAQ検索は`faq.html?q=...&course=...`へ渡す。
- FAQbotとその`returnUrl`へ検索語`q`を引き継がない。
- `js/faq.js`と`js/faq_page.js`は、検索入口の変更だけを理由に変更しない。

## レスポンシブデザイン

- 基準はMacBook Airの画面を左右半分にした程度の表示領域。
- 固定幅だけにせず、`min()`、`max()`、`clamp()`などで広い画面と狭い画面へ対応する。
- 極端に広い画面では本文が間延びしないよう最大幅を設ける。
- 390px程度の画面幅でも横スクロールや操作不能を起こさない。
- タップ対象は原則44px程度を確保する。
- ホバーだけを必須操作にしない。
- `prefers-reduced-motion`を尊重する。

## テーマと文字サイズ

サイトには次の表示設定がある。

- テーマ: 自動、ライト、ダーク
- 文字サイズ: 標準、大、大きめの3段階

- 新しい部品は既存のCSS変数を使い、すべてのテーマへ対応する。
- OSの`prefers-color-scheme`だけを条件にせず、`data-theme`による手動設定にも対応する。
- テーマ選択UIは現在モードのアイコンを通常表示し、選択肢表示中は3つすべてを表示する。
- 通常表示の選択アイコン背景は透明にする。
- 選択肢表示中の選択済みアイコンは背景で強調する。
- 文字サイズを大きくしてもdialog、Dock、ナビ、ボタンが欠けないようにする。

## アクセシビリティ

- キーボード、Tab、Shift+Tab、Enter、Escapeで操作可能にする。
- `:focus-visible`を明確に表示する。
- アイコンだけのボタンには内容が分かる`aria-label`を付ける。
- モーダルを閉じたら、原則として開いたボタンへフォーカスを戻す。
- 教材画像には内容に即した`alt`を設定する。
- 適切なaltを機械的に判断できない場合は、推測せずユーザー確認事項として挙げる。
- 操作や学習に必要な情報を画像だけへ置かない。
- サイト全体への`aria-live`追加は現在保留。明示的な依頼なしに一括導入しない。

## 画像

- 表示が遅いという実測がない限り、教材画像を一律に縮小版へ置き換えない。
- 大きめの原画像を維持し、CSS上の表示サイズ、`loading="lazy"`、`decoding="async"`など安全な改善を優先する。
- 透明背景画像に単純なグレー背景を敷くだけで、ライト、ダーク両方の可読性を解決したことにしない。
- 単純なフローチャートはSVG化を検討する。
- `ss3x`系画像は現状の教材として許容されているため、理由なく置き換えない。

## 教材内容の編集方針

- 技術的により厳密な表現へ直す場合でも、授業段階と教科書の記述を優先する。
- ユーザーが意図的に残している難易度、文章量、固定値を一般論だけで削除しない。
- 内容判断が必要な箇所と、安全に一括修正できる箇所を分ける。
- リンク切れ、壊れた画像タグ、不正なHTML構造、明確なアクセシビリティ不足は優先的に修正する。
- 教材本文の意味が変わる修正は、根拠と影響を示して確認する。

### HTML実習

- 授業で主に扱う範囲は1-1〜1-7、2-1〜2-4。
- CSSは概要紹介に留め、詳しい内容は自学を促して自由制作へ進む。
- 最終課題は修学旅行先の観光促進サイト制作。
- 1ページ構成を基本としてよいが、複数ページも許可する。
- 初回制作なので細かい制限を増やさず、ある程度自由に制作させる。
- `ul`、`ol`のネストや`table`は、試験と翌年度のPython学習につながるため残す。
- `html15`はページ内遷移を理解できる文章量を保つ。
- 固定幅、固定高さの写経例は授業中の注意で補う方針。
- `html17`の`table border="1"`はCSS学習前の一時的な確認方法として、小さな補足に留める。
- `html21`は読むことを中心としたページ。
- `html18`はセマンティックHTML等の応用紹介。
- `html25`は値を変えて挙動を確認できるCSS応用ページ。
- 応用ページでは実例と変更結果を自分で試せる構成を優先する。

### Python講座

- 4-1〜5-3を重点的に確認する。
- クイックソートは発展教材であり、必修扱いにしない。
- 「試行回数が増えるほど必ず理論値に近づく」という表現は、教科書準拠として現状維持する。
- Matplotlibで未説明のメソッドを使う場合は、初出箇所に短い説明を加える。
- サイトとColab原本でメソッド名、用語、表記を揃える。
- サーチ、ソートのデモは、注目中の要素、比較対象、確定済み要素を視覚的に区別する。
- 要素数が少ない場合に配列表示が間延びしないよう最大幅を設ける。
- フローチャートのデモは既存配列デモと独立してよい。
- 開始前はフローチャート全体を通常表示し、開始後だけ現在処理を強調して他を薄くする。
- 初期状態へ戻せる操作を用意する。

## 最低限の検証

変更内容に応じて、次を実行する。

### JavaScript

```sh
node --check js/変更ファイル.js
```

複数ファイルを変更した場合は対象範囲の全ファイルを確認する。

### 共通

```sh
git diff --check
```

### 検索関連

```sh
node scripts/test-site-search.mjs
node scripts/generate-search-index.mjs --check
```

### スライド関連

```sh
node scripts/generate-slide-pages.mjs --check
node scripts/validate-slide-content.mjs
```

### ブラウザ確認

UI変更時は可能な限り次を確認する。

- デスクトップ幅と390px幅。
- マウスとタッチ。
- Tab、Shift+Tab、Enter、Escape。
- フォーカス復帰。
- ライト、ダーク、自動。
- 3段階の文字サイズ。
- 左目次、LessonDock、dialogの排他制御。
- 画像、JSON、CSS、JavaScriptの404がないこと。

ブラウザ確認ができない場合は、実施したかのように報告せず、未確認事項として明記する。

## 現在保留中の判断

次は一時的な判断である可能性があるため、変更依頼と直接関係する場合はユーザーへ確認する。

- `ss41`の保留事項。
- `answer1-4.html`、`answer1-5_1.html`の修正。
