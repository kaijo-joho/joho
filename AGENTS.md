# AGENTS.md

## プロジェクト概要

このリポジトリは、海城中学高等学校 情報科の教材サイトである。

- 公開方式はGitHub Pagesによる静的サイト。
- 主な教材はHTML/CSS、Illustrator、スプレッドシート、Python。
- 主な利用環境は生徒用MacBook Air、SafariまたはGoogle Chrome。
- 教材内容の正確さだけでなく、授業時間、学習段階、試験範囲、配付物との整合性を重視する。
- ユーザーとのやり取りと作業報告は原則として日本語で行う。

## 作業開始時

作業前に必ず次を確認する。

```sh
git status --short --branch
git diff --stat
git diff --cached --stat
git log -1 --oneline --decorate
```

- 差分があるファイルは、作業対象と重なる箇所を`git diff -- <path>`と`git diff --cached -- <path>`で確認する。
- 既存の未コミット差分をユーザーの作業として扱い、失わない。
- 関係のない変更を修正、整形、コミットしない。
- `git reset --hard`、`git checkout --`などの破壊的操作は禁止。
- 現在のブランチと`origin`との差を確認してからコミット、pushする。
- 大規模な機械置換より、対象を限定した小さな差分を優先する。

## Git運用

- コミット、pushは、現在の依頼または明示された継続的な許可がある場合に限る。
- 過去の特定スレッドだけで与えられたコミット、push許可を、恒久的な許可と解釈しない。
- 新規ファイルを追加する作業では継続的な許可があるものとし、ユーザーから個別に止められていない限り、必要な検証後に今回の作業分をコミットしてpushする。
- 次の事項はユーザーの判断が必要なものとして扱い、未確認のままpushしない。
  - 教材本文の意味、難易度、学習範囲が変わる。
  - ページや教材の公開・非公開が変わる。
  - 公開済みURLに関わるファイル名変更、移動、削除を行う。
  - 新しいHTMLを`ページ一覧`へ登録するか判断が分かれる。
  - 大量変更や、生成結果に依頼範囲外の差分が含まれる。
  - 外部原本と整合しない可能性がある。
- ユーザーの判断が必要そうな事項がほかにも残る場合は、推測で決めずにpushを保留する。論点、選択肢、影響をユーザーへ確認し、回答を反映して検証してからpushする。
- 新規ファイルをpushする前に、秘密情報、認証情報、個人情報、公開前の試験・解答、第三者著作物の利用条件、巨大ファイル、一時ファイルが含まれていないことを確認する。判断できない場合はpush前にユーザーへ確認する。
- コミットには今回の作業ファイルだけを含める。
- push前に`origin`の更新を確認し、force pushは行わない。
- GitHub Pagesは`main`へのpush後の自動反映を基本とし、通常は別の公開手順を追加しない。公開教材に関わる変更は、push成功後に公開サイトへの反映を確認して完了とする。
- PR作成やmainへのマージは、明示的に依頼された場合だけ行う。
- 作業後は変更ファイル、実装内容、検証結果、未確認事項を報告する。コミット、pushを実施した場合はコミットSHAとpush先も報告する。

## 原本と生成物

- 外部原本へアクセスできない場合は、確認または更新したかのように報告せず、未確認事項として明記する。

### 「配付ファイル一覧」GSSと`js/pages.js`

`js/pages.js`は、「配付ファイル一覧」Googleスプレッドシート内の`ページ一覧`、`ファイル一覧`、`DLファイル一覧`、`impクイズ管理_cfg`を入力として生成される。

配付ファイル一覧GSS（`ページ一覧`シート）:

https://docs.google.com/spreadsheets/d/1d4--Rcap1BmZJPWJoEuK9teqoGn6Jyjzn_Q1FcDTvqE/edit?gid=70227298#gid=70227298

- `ページ一覧`はリポジトリ内のHTMLファイル全件を管理する台帳ではない。配付用、サンプル、解答、補助ページなど、`ページ一覧`にないHTMLファイルが存在してよい。
- `js/pages.js`は生成物なので直接編集しない。
- 新しいHTMLを`ページ一覧`で管理するページとして追加するときは、同シートへ`release: false`で行を追加してから、GSSの生成処理で`js/pages.js`を再生成する。公開は確認後に`release: true`へ変更して再生成する。
- `release: false`はナビゲーションや検索などへ掲載しないための設定であり、アクセス制御ではない。HTMLファイルをGitHub Pagesへpushすると、URLを直接指定して閲覧できる可能性があるため、本当に非公開とすべき内容はpushしない。
- ページの名称変更、公開状態、前後関係、配付物との関連付けも、`ページ一覧`を変更して`js/pages.js`を再生成する。
- `practiceFile`、`exampleFile`、`questionFile`には`ファイル一覧.id`、`dlFile`には`DLファイル一覧.id`、`quizForm`には`impクイズ管理_cfg.quizId`を指定する。URLを直接入力しない。
- `back`と`next`には`ページ一覧.id`を指定する。複数指定できる欄は半角カンマで区切り、IDの前後へ空白を入れない。
- `教材ファイル一覧`をファイル実体と生成関係の正本、`配布設定一覧`を公開・配付設定の正本とする。`ファイル一覧`と`元ファイル一覧`はこれらから作る既存GAS向け互換ビューなので、通常の教材更新では直接編集しない。
- `ファイル一覧`は互換ビューだが、現行の`pages.js`生成処理が教材参照の結合に使用する。正本を変更した後は、同じIDが互換ビューへ正しく反映されたことも確認する。
- `ページ一覧.release: true`をページ公開、配付物側の`release: true`を教材リンク公開の基準として扱う。`教材ファイル一覧.managed`は自動生成の対象かどうかを示す値であり、公開判定には使わない。
- `pages.js`から生成されるリンクを各HTMLへ重複してハードコードしない。
- データの責務、参照関係、更新手順の詳細は`docs/page-list-data-flow.md`も確認する。

### スライド由来教材

- `data/slides/<ID>.json`と`img_slide/*.png`は生成物であり、原則として直接編集しない。
- `config/slide-pages.json`に登録された全文型ページは直接編集せず、同設定、`templates/slide-page.html`、Googleスライド側の原本を変更して再生成する。
- 発表者ノートは公開文章として扱い、教員専用メモを混在させない。
- ハイブリッドページでは概念説明をHTML、操作手順をスライドJSONに置く。
- `data-slide-section`はスライド側のセクション名と完全一致させる。
- 詳細な公開手順は`docs/slide-html-publishing.md`も確認する。

生成と検証:

```sh
node scripts/generate-slide-pages.mjs
node scripts/generate-slide-pages.mjs --check
node scripts/validate-slide-content.mjs
```

### Colabノートブック

Colab原本は別プロジェクトの`~/Documents/Colab/`に同期されている。

- 教材サイトとノートブックで説明、表記、コードを変更する場合は原本との整合性を確認する。
- サイト上の表示だけを直して原本を放置しない。
- 別リポジトリへの変更、pushは、その作業範囲が明示されている場合だけ行う。

### HTML配付ファイル

- HTML実習ページを変更した場合、対応する`html/`内の配付用ファイルも確認する。
- 課題の意図に関係する修正は、教材ページと配付ファイルで揃える。
- サンプル、解答ファイルは公開教材ページとは別物として扱い、依頼なく一括修正しない。

## 教材ページの種別と共通デザイン

通常の教材ページは、学習形態によって次の2種類に分ける。

- 実習ページ: `py`、`ss`、`il`、`html`シリーズ。
- 座学ページ: `dr`、`lc`シリーズ。

- ヘッダー、ナビゲーション、LessonDock、本文幅、テーマ、文字サイズ、アクセシビリティなどの基本デザインは、実習・座学で共通にする。
- 教材固有の図、シミュレーション、回路エディタ、問題UIなどにはシリーズ別のCSSとJavaScriptを使用してよい。個別設定は必要な範囲へ限定し、共通部分をページごとに複製しない。
- 複数ページで必要になる差異は、可能な限りdata属性、CSS変数、共通設定、共通コンポーネントとして表現する。ページ固有の判断は`docs/page-specific-editing-notes.md`へ記録する。
- 座学の概念説明ページは、本文全体を縦に並べるのではなく、現在の`dr31.html`のように内容を順番にめくるスライド形式を基本とする。
- 自由操作、問題演習、エディタなどは学習内容に適した専用UIを優先し、無理にすべてをスライド内へ収めない。
- ここでいうスライド形式はHTMLページ内の表示・操作方式であり、「スライド由来教材」のGoogleスライド生成フローとは別である。表示形式だけを理由に`config/slide-pages.json`へ登録しない。
- `lc`シリーズはスライド形式への対応途中である。現状を完成仕様とみなさず、座学ページを整備するときは`dr`シリーズを基準に段階的に揃える。
- `lc`シリーズのスライド対応を、無関係な修正へ便乗して一括実施しない。変換範囲や専用UIとの両立に判断が必要な場合は、ユーザーへ確認する。
- 座学用のスライド操作を`dr`と`lc`へ展開するときは、同じ機能をシリーズ別に重複実装せず、共通化したうえで必要な差異だけを設定する。
- 座学ページのUI変更では、前後移動、現在位置、URLハッシュ、キーボード操作、フォーカス移動、スライド内スクロール、JavaScript無効時の本文順を確認する。専用の操作部品があるページでは、そのキーボード・ドラッグ・タッチ操作も妨げないことを確認する。

## JavaScript構成

共通処理は`js/main.js`から段階的に初期化される。概ね次の順序を保つ。

1. `head.js`、`pages.js`、`faq.js`
2. DOM準備
3. `script.js`による共通レイアウト
4. `nav.js`、`highlight.js`、検索コア
5. 検索UI、`lesson_dock.js`
6. `script_pages.js`
7. 動的コンテンツを含む最終ハイライト処理

- 任意機能の失敗でサイト全体を停止させない。
- 同じ処理を複数ファイルへ重複実装しない。
- 動的に生成される内容を考慮し、初期化順序を変更するときは影響範囲を確認する。
- グローバル関数を増やす場合は必要最小限にする。

### `highlight.js`

- Python、HTML、CSS、表計算、実行結果などのコード表示を処理する。
- 入力内容を必ずエスケープし、教材コードを任意HTMLとして実行させない。
- 文字列、コメント、HTML属性、CSSリテラルなどが後続の正規表現で再処理されないよう注意する。
- `pre code`の構造がページごとに違う場合でも壊れないようにする。
- 見出し番号はすべての`h2`へ無条件に付与しない。
- 番号を付けない見出しには`data-skip-numbering`を使用する。
- 「確認問題」「次回は…」や目次の「はじめに」は本文の章番号へ含めない。

## LessonDockとナビゲーション

- LessonDockの既存機能を壊さず、追加機能の失敗を他のボタンへ波及させない。
- 「次回」アイコンは遷移先が1ページだけの場合に限り、クリックで直接移動する。
- 複数の次回ページがある場合は、アイコンクリックだけで勝手に遷移させない。
- パネル内にフォーカスがある間は、`mouseleave`の遅延処理で閉じない。
- 左目次、LessonDockパネル、検索dialogなどは`joho:overlay-open`によって排他的に開く。
- トップページでは左目次を開かない。
- 左目次の4pxの細いハンドルは意図的なデザインなので、太くしない。

## サイト内検索

- 検索はブラウザ内で完結させ、外部検索サービスへ検索語を送らない。
- `data/search-index.json`は生成物なので直接編集しない。
- `js/pages.js`、公開HTML、`data/slides/*.json`を変更したら検索索引を再生成する。
- 詳細は`docs/site-search.md`も確認する。

```sh
node scripts/generate-search-index.mjs
node scripts/generate-search-index.mjs --check
node scripts/test-site-search.mjs
```

- 検索対象は`pages.js`の`release: true`ページのみ。
- FAQ、中継ページ、テスト、未公開ページ、配付用サンプル、解答は索引へ含めない。
- 検索結果は文字列結合による`innerHTML`へ渡さず、テキストノードとして構築する。
- FAQ検索処理やFAQbot URL生成をLessonDockやサイト検索側へ重複実装しない。
- FAQ検索は`faq.html?q=...&course=...`へ渡す。
- FAQbotとその`returnUrl`へ検索語`q`を引き継がない。
- `js/faq.js`と`js/faq_page.js`は、検索入口の変更だけを理由に変更しない。

## レスポンシブデザイン

- 基準はMacBook Airの画面を左右半分にした程度の表示領域。
- 固定幅だけにせず、`min()`、`max()`、`clamp()`などで広い画面と狭い画面へ対応する。
- 極端に広い画面では本文が間延びしないよう最大幅を設ける。
- 390px程度の画面幅でも横スクロールや操作不能を起こさない。
- タップ対象は原則44px程度を確保する。
- ホバーだけを必須操作にしない。
- `prefers-reduced-motion`を尊重する。

## テーマと文字サイズ

サイトには次の表示設定がある。

- テーマ: 自動、ライト、ダーク
- 文字サイズ: 標準、大、特大の3段階

- 新しい部品は既存のCSS変数を使い、すべてのテーマへ対応する。
- OSの`prefers-color-scheme`だけを条件にせず、`data-theme`による手動設定にも対応する。
- テーマ選択UIは現在モードのアイコンを通常表示し、選択肢表示中は3つすべてを表示する。
- 通常表示の選択アイコン背景は透明にする。
- 選択肢表示中の選択済みアイコンは背景で強調する。
- 文字サイズを大きくしてもdialog、Dock、ナビ、ボタンが欠けないようにする。

## アクセシビリティ

- キーボード、Tab、Shift+Tab、Enter、Escapeで操作可能にする。
- `:focus-visible`を明確に表示する。
- アイコンだけのボタンには内容が分かる`aria-label`を付ける。
- モーダルを閉じたら、原則として開いたボタンへフォーカスを戻す。
- 教材画像には内容に即した`alt`を設定する。
- 適切なaltを機械的に判断できない場合は、推測せずユーザー確認事項として挙げる。
- 操作や学習に必要な情報を画像だけへ置かない。
- サイト全体への`aria-live`追加は現在保留。明示的な依頼なしに一括導入しない。

## 画像

- 表示が遅いという実測がない限り、教材画像を一律に縮小版へ置き換えない。
- 大きめの原画像を維持し、CSS上の表示サイズ、`loading="lazy"`、`decoding="async"`など安全な改善を優先する。
- 透明背景画像に単純なグレー背景を敷くだけで、ライト、ダーク両方の可読性を解決したことにしない。
- 単純なフローチャートはSVG化を検討する。

## 教材内容の編集方針

- 技術的により厳密な表現へ直す場合でも、授業段階と教科書の記述を優先する。
- ユーザーが意図的に残している難易度、文章量、固定値を一般論だけで削除しない。
- 内容判断が必要な箇所と、安全に一括修正できる箇所を分ける。
- リンク切れ、壊れた画像タグ、不正なHTML構造、明確なアクセシビリティ不足は優先的に修正する。
- 教材本文の意味が変わる修正は、根拠と影響を示して確認する。
- ページ、講座、教材群に固有の判断は`docs/page-specific-editing-notes.md`へ記録する。
- 同文書は対象となる教材を変更するときだけ該当箇所を確認し、無関係なページへ一律に適用しない。

## 最低限の検証

変更内容に応じて、次を実行する。

### JavaScript

```sh
node --check js/変更ファイル.js
```

複数ファイルを変更した場合は対象範囲の全ファイルを確認する。

### 共通

```sh
git diff --check
```

### 検索関連

```sh
node scripts/test-site-search.mjs
node scripts/generate-search-index.mjs --check
```

### スライド関連

```sh
node scripts/generate-slide-pages.mjs --check
node scripts/validate-slide-content.mjs
```

### 論理回路関連

`js/logic-*.js`、`lc*.html`、`css/logic-circuits.css`を変更した場合に実行する。

```sh
node scripts/test-logic-core.mjs
node scripts/test-logic-applications.mjs
```

### 音のデジタル表現関連

`js/sound-*.js`、`js/dr-slide-deck.js`、`dr31.html`、`dr32.html`、`css/digital-representation.css`を変更した場合に実行する。

```sh
node scripts/test-sound-core.mjs
node scripts/test-sound-pages.mjs
```

### ブラウザ確認

UI変更時は可能な限り次を確認する。

- デスクトップ幅と390px幅。
- マウスとタッチ。
- Tab、Shift+Tab、Enter、Escape。
- フォーカス復帰。
- ライト、ダーク、自動。
- 3段階の文字サイズ。
- 左目次、LessonDock、dialogの排他制御。
- 画像、JSON、CSS、JavaScriptの404がないこと。

ブラウザ確認ができない場合は、実施したかのように報告せず、未確認事項として明記する。
