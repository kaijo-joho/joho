# ページ・教材別の編集メモ

この文書には、特定の講座、教材群、ページだけに適用する編集判断を記録する。リポジトリ全体の編集・運用規則は`AGENTS.md`に従う。

- 対象となる教材を変更するときだけ、該当する節を確認する。
- ここにある判断を無関係なページへ一律に適用しない。
- 保留事項の内容が不明な場合は推測せず、ユーザーへ確認する。

## HTML実習

### 講座全体

- 授業で主に扱う範囲は1-1〜1-7、2-1〜2-4。
- CSSは概要紹介に留め、詳しい内容は自学を促して自由制作へ進む。
- 最終課題は修学旅行先の観光促進サイト制作。
- 1ページ構成を基本としてよいが、複数ページも許可する。
- 初回制作なので細かい制限を増やさず、ある程度自由に制作させる。
- `ul`、`ol`のネストや`table`は、試験と翌年度のPython学習につながるため残す。
- 固定幅、固定高さの写経例は授業中の注意で補う方針。
- 応用ページでは、実例と変更結果を生徒自身が試せる構成を優先する。

### ページ別

- `html15.html`は、ページ内遷移を理解できる文章量を保つ。
- `html17.html`の`table border="1"`はCSS学習前の一時的な確認方法として扱い、補足を加える場合も小さく留める。
- `html18.html`は、セマンティックHTMLなどの応用紹介として扱う。
- `html21.html`は、読むことを中心としたページとして扱う。
- `html25.html`は、値を変えて挙動を確認できるCSS応用ページとして扱う。
- `html/sample2-1.html`は配付対象にしない。

## Python講座

### 講座全体

- 4-1〜5-3を重点的に確認する。
- クイックソートは発展教材であり、必修扱いにしない。
- Matplotlibで未説明のメソッドを使う場合は、初出箇所に短い説明を加える。
- サイトとColab原本でメソッド名、用語、表記を揃える。
- サーチ、ソートのデモは、注目中の要素、比較対象、確定済み要素を視覚的に区別する。
- 要素数が少ない場合に配列表示が間延びしないよう最大幅を設ける。
- フローチャートのデモは既存配列デモと独立してよい。
- 開始前はフローチャート全体を通常表示し、開始後だけ現在処理を強調して他を薄くする。
- 初期状態へ戻せる操作を用意する。

### ページ別

- `py21.html`のif文フローチャートでは、真・偽の処理ボックスを接続して見せず、分岐先として区別できる中央の余白を保つ。
- `py53.html`の試行回数と理論値に関する表現は、教科書準拠として現状維持する。

## スプレッドシート教材

- `ss3x`系画像は現状の教材として許容されているため、理由なく置き換えない。

## 論理回路（lc）シリーズ

- `lc02.html`は共通スライド基盤を維持しつつ、通常URLで回路エディタを初期表示する。導入説明は前のスライドから確認でき、見出しハッシュで開いた場合は指定先を優先する。
- エディタのツールバーは上段を編集操作、下段を部品追加とし、DOM・Tab順もこの順にする。狭い画面では各段内で折り返す。自由編集の整列アイコンは`js/logic-layout.js`で回路全体を入力・ゲートの段・出力の順に配置する。左右の段間隔をそろえ、縦方向は列の順序と最小間隔を保ちながら、直線になる配線数、配線の縦差、基準位置への近さの順で候補を選ぶ。ゲートから出力へ、最後に入力を調整し、未接続入力との間隔も維持する。端子の高さはエディタから`inputOffset`で渡し、ゲート形状を配置処理へ重複定義しない。配置だけを変更し、接続・種類・名前・入力値・真理値表を維持する。未完成回路と複数出力も扱い、キャンバスに収まらないときは変更せず案内する。整列はUndoで戻せ、繰り返しても位置・履歴を増やさない。
- 真理値表は初期表示し、エディタと表の境界の「▶」で折りたたみ、「◀」で再表示する。閉じたときは境界ボタンを残して回路を広げ、再表示時は現在の入力値の行を強調する。390px幅では回路・境界・表の順に縦へ並べる。開閉は回路の値・保存状態・Undo履歴に含めず、`hidden`と`aria-expanded`でキーボード・タッチでも操作できるようにする。ドラッグ中は開閉で座標系を変えない。
- 自由編集では部品の追加・移動中に、接続済み配線が直線になる端子の高さ、同じ行・列の隣接2部品と中心が等間隔になる位置、他の部品の中心の順で吸着を優先する。等間隔は2部品の間と外側への延長を扱い、入力・ゲート・出力を区別しない。逆方向の配線や途中の部品を貫通する直線は吸着候補にしない。取得は画面上7px以内、保持は14px以内とし、破線と「直線」「等間隔」のラベルで示す。部品に重なる位置には吸着せず、Alt（Option）を押しながらドラッグすると一時的に解除する。Escape・pointercancel・pointer capture喪失では元の位置に戻し、クリック時の微小な揺れでは動かさない。補助線と吸着状態は保存データ・SVG・PNGへ含めない。`enableAlignment`で自由編集だけに有効化し、`lc03`では自動整列・吸着を有効にせず、採点は変更しない。
- 自由編集の入力はA・Bから開始し、C・Dはツールバーで順に追加する。追加時に既存部品を動かさず、回路例に必要な入力は読み込み時に補う。自由編集では入力も×・選択を削除・Deleteで削除でき、接続配線と値を含めてUndo/Redoで復元する。削除した入力は追加し直せる。`lc03`の組み立て問題で指定された入力は削除できない。
- 自由編集は出力Fから始め、ツールバーの「＋ 出力」で増やす。複数のときは追加順にF₁・F₂…と命名し、真理値表は出力ごとの列を持つ。出力を削除したら連番を詰め、1つになったらFに戻す。最後の1出力は削除不可とし、追加・削除・接続・名前はUndo/Redoで復元する。全消去・テンプレートの読み込みでは出力をFに戻し、保存した回路を読み込む場合はその出力構成を復元する。複数出力の解析は`allowMultipleOutputs`による明示的な有効化とし、`lc03`の判定は従来の単一出力制約を維持する。回路の完成判定と真理値表には全出力の入力・到達ゲートを使い、未接続の出力があれば画像出力不可とする。編集データの保存は未完成でも可能とする。
- パレットのAND・OR・NOTはクリック・Enterで追加でき、回路内へドラッグすればドロップ位置に配置する。ドラッグ中はプレビューだけを表示し、回路外へのドロップ・Escape・pointercancelでは回路や履歴を変えない。パレットの安定したボタンでpointer captureし、ドロップ直後のclickによる二重追加を防ぐ。
- 配線を選ぶと接続元・接続先の端子を強調し、左端を別の出力端子へ、右端を別の入力端子へドラッグして付け替えられる。分岐元が共通でも選んだ1本だけ変更する。接続済み入力端子への新規接続・付け替えは、その端子の古い配線を置き換える。循環・逆向き・存在しない端子を検証した後に一括変更し、失敗時は両配線を保持する。1端子1配線、Enterでの接続、キャンセル、1回のUndoで置き換え前へ戻る操作を維持する。
- 選択したAND・ORゲートはツールバーから相互に交換でき、位置・接続・部品IDを保持する。入力数が異なるNOTへの交換は行わない。自由編集では交換と選択削除もアイコンにし、ホバーの説明・読み上げ名と44pxの操作領域を保つ。
- 配線は水平・垂直とし、重なりや他の部品の通過を避ける。別信号どうしの交差を避けるためだけに余分な折れ曲がりを増やさず、●は同一信号の分岐点にだけ付ける。
- 自由編集では、選択した配線の内部線分に44 CSS pxの操作領域を持つハンドルを表示する。縦線分は左右、横線分は上下へドラッグ・矢印キーで移動でき、Shiftで移動量を増やす。ハンドル上のDeleteは配線を削除せず自動経路へ戻す。部品貫通や別信号の配線との重なりは拒否し、両端の接続と別の分岐の設定は維持する。変更は内部折れ点`wire.bends`へ保存し、Undo/Redo・ブラウザ保存・JSON保存・画像出力で保持する。Escape・pointercancel・capture喪失は途中変更を破棄する。部品移動後も両端の高さへ追従し、部品に重なる間は自動経路へ退避する。端子の付け替えと自動整列では手動経路をリセットし、Undoで復元できる。ハンドルは保存図に含めず、`enableWireEditing`でlc02だけに有効化する。
- 操作説明はツールバーの「？」をクリック・タップ・Enterで開くモーダルにまとめる。導入スライドの説明と同じ`data-lesson-supplement-dialog`を共用し、非表示スライドに隠れないようbody直下へ移す。開閉・フォーカス・排他制御は座学共通基盤を使用し、ページ別に複製しない。内容は部品・配線・表示/整列・保存の4分類とする。保存・読み込み・画像出力も共通のダイアログ用スタイルを使い、Escape・背景・閉じるボタンで閉じて呼出元へフォーカスを戻す。他のメニューやスライド移動時は閉じ、ダイアログ内のキー操作を回路編集へ渡さない。
- エディタの高さは共通基盤が確保する残りの高さへ追従させ、独自の固定高さ計算で縮めない。390px幅ではボタンを折り返し、回路内をスクロールして操作し、真理値表は下へ配置する。
- 自由編集の保存・読み込みはツールバーのアイコンから行う。`js/logic-storage.js`が`localStorage`の`joho.logic-circuits.v1`へ名前付きで最大30件を保存し、`js/logic-workbench-files.js`が保存・読み込み・画像出力のUIを担当する。保存対象は部品と位置、配線、入力一覧・値で、編集中の選択やDOMは含めない。未完成回路・複数出力も復元可能とする。読み込みには保存した回路と従来の4テンプレートをまとめ、独立した回路例パネルは置かない。更新は保存画面の「上書き保存」、追加は「別の回路として保存」で明示し、同名の別データは自動上書きしない。保存データの削除には対象名を示して確認する。保存先は同じブラウザ内であり、端末間同期を行わず、閲覧データ削除で消えることを画面へ明記する。
- 全消去・別回路の読み込み前は、未保存の変更があれば「保存して続ける／保存せず続ける／キャンセル」を選ばせる。入力値の変更も対象とし、保存済みデータが別タブで削除・更新された場合や読めない場合も未保存として扱う。保存失敗時は置換を実行しない。読み込み・全消去前の状態は真理値表から設定した入力値も含めUndoで戻せるようにする。保存データは版・件数・サイズ・ID・部品・端子・座標・入力値・循環を検証し、不正データや容量不足で既存の保存内容を消去しない。
- 保存ダイアログには「ファイルに保存」も置き、`format: joho.logic-circuit`・`version: 1`・名前・snapshotを持つ`.logic.json`をダウンロードする。読み込みダイアログはローカルファイル、ブラウザ保存、テンプレートをまとめる。JSONは1 MiB以内に制限し、形式・版・回路・折れ点を検証してから置き換える。読み取り中のキャンセルや別メニューへの移動、不正ファイルでは回路を変更しない。ブラウザ保存領域が使用不可でもファイル保存・読み込みは利用できる。ダウンロード開始だけでは保存完了と断定しないため、全消去等の直前にファイル保存を選んだ場合は「保存を確認して続ける」を明示的に押してから進む。
- 自由編集のツールバーの「0/1」で、エディタと画像出力の値・信号色をまとめて切り替える。初期状態は表示、非表示でも実際の入力値と真理値表は維持し、表示設定自体をUndo・保存snapshot・未保存判定に含めない。SVG・PNGの出力ダイアログには形式だけを置き、0/1のチェックボックスは置かない。完成回路の現在の配置・手動経路を共通ルーターから取得し、共有ゲートを出力ごとに複製せず、選択枠など編集UIは含めない。未使用の入力は保存図から省く。保存図の入力・出力は枠なしの点と名前で示し、ゲート端子の点は描かず、配線の分岐点●とNOTの否定を表す○は残す。SVGは実際の改行と単一のXML名前空間を持たせ、描画用CSSの既定値をSVG属性へ展開して外部CSS・CSS変数に依存させない。両形式とも白背景で、PNGは通常2倍解像度・長辺4096pxまでとする。PNG変換に失敗した場合は一時URLと処理中状態を解除し、ダイアログでエラーを伝えて再試行できるようにする。
- 編集操作と経路選択の回帰検証は`node scripts/test-logic-editor.mjs`を実行する。ブラウザでは選択済み配線の両端、分岐、ゲート交換、入力追加、履歴、説明・回路例の開閉と全画面時の高さも確認する。
- 整列の純粋な配置処理は`node scripts/test-logic-layout.mjs`、整列・中心／直線／等間隔吸着・真理値表の折りたたみ・上下段の操作は下記と同じブラウザ環境で`node scripts/test-logic-alignment-browser.mjs`を実行する。再整列の安定性、端子の高さを考慮した直線経路、Undo、接続と値の保持、吸着の開始・解除・キャンセル、保存と画像出力、390px幅を確認する。2026年9月11日、Chrome・Playwright WebKitでこれらと既存編集・保存の回帰テストが通り、折りたたみのキーボード操作・全テーマ・文字サイズ・全画面も確認した。Chromeでは390px幅の実タッチイベントによる真理値表の開閉・中心／直線／等間隔吸着・補助線・Undoも確認した。Safariアプリとスマートフォン実機は未確認。
- 保存データの回帰検証は`node scripts/test-logic-storage.mjs`、保存・読み込みUIは下記と同じ環境で`node scripts/test-logic-storage-browser.mjs`を実行する。再読み込み後の複数回路の復元、上書き・別名、破損データ、保存失敗時の全消去防止、削除確認、Undo、フォーカス復帰を確認する。
- 境界ボタンの配置・モーダルヘルプは`node scripts/test-logic-workbench-ui-browser.mjs`、手動配線は`node scripts/test-logic-bends-browser.mjs`で検証する。後者は実際のドラッグとキー操作、分岐点、Undo/Redo・キャンセル、ダウンロードJSONからの復元、SVG経路の一致を確認する。
- タッチ中の追跡先は再描画で消えないSVGとし、配線・部品のドラッグと空白のパンを分ける。空白の縦スワイプが回路内の端へ達したら、外側のスライドへ移動量を引き継ぐ。2026年9月9日、Chrome・WebKitで390〜1920px幅、全テーマ・文字サイズ、全画面、マウス・キーボード操作、タップを確認した。Chromeではタッチによる配線左端の付け替えと空白スワイプも確認した。Safariアプリやスマートフォン実機での確認は未実施。
- ブラウザ回帰検証は、ローカルHTTPサーバーとPlaywrightを用意して`JOHO_TEST_URL=http://127.0.0.1:8765/ node scripts/test-logic-editor-browser.mjs`で実行する。依存環境は必要に応じて`NODE_PATH`で指定する。実ファイルを保存してSVGのXML解析・描画属性、PNGのシグネチャ・寸法・画素を検証し、一時ディレクトリへ検証用画像を残す。2026年9月11日、Chrome・Playwright WebKitで編集操作、複数出力の追加・削除・Undoと真理値表、値の表示・非表示を切り替えた保存図、共有ゲート・分岐点・NOT記号、390・720・1800px幅、全テーマ・文字サイズ、全画面を確認した。Chromeでは390px幅のタッチによるゲート追加とタップも確認した。IllustratorはMacがロックされていたため実機での読み込みは未確認。

## Digital Representation（dr）シリーズ

### 番号体系

| ID | 内容 |
| --- | --- |
| `dr01` | デジタルとアナログ・データ量 |
| `dr02` | 2進数と16進数 |
| `dr11` | 数値のデジタル表現(整数) |
| `dr12` | 数値のデジタル表現(実数) |
| `dr13` | 演算誤差 |
| `dr21` | 文字のデジタル表現 |
| `dr22` | 色のデジタル表現 |
| `dr31`〜 | 音のデジタル表現 |
| `dr41` | 画像のデジタル表現 |
| `dr42` | 動画のデジタル表現 |
| `dr43` | ディスプレイとプリンタ |
| `dr51` | データの圧縮とランレングス法 |
| `dr52` | ハフマン符号化 |
| `dr61`〜・`dr71`〜 | 将来拡張用 |
| `dr81`〜 | 将来拡張用 |
| `dr91`〜 | 総合演習用 |

- 各単元は、原則として「基本 → 自由操作・実験 → 問題演習」の流れで構成する。
- 2026-09-13のユーザー指定により、前半は`dr01`・`dr02`・`dr11`・`dr12`・`dr13`・`dr21`・`dr22`の7ページとする。`dr01`にデジタルとアナログの概念、bit・B、情報の通り数、必要なビット数、単位換算をまとめ、`dr02`で2進数・10進数・16進数の相互変換を扱う。
- 2026-09-12のユーザー指定により、画像・動画・ディスプレイとプリンタを`dr41`〜`dr43`にまとめる。ベクタとラスタの比較は`dr41`内に1枚程度のスライドとして収める方針とし、独立した`dr42`にはしない。色の詳しい説明は`dr22`で扱う。同日の続く指定で、圧縮は`dr51`からの2回分とする。旧計画の`dr71`割当は使用しない。
- 座学・概念説明をウェブ化するページは、縦に全章を並べるのではなく、次へ進むスライド形式を基本とする。問題演習は内容に適したUIを優先し、無理にスライド形式へ統一しない。
- `dr31.html`と`dr32.html`は手書きHTMLへ座学共通基盤の`css/lesson-slide-deck.css`と`js/lesson-slide-deck.js`を適用したページであり、どちらもGoogleスライド由来教材の生成対象ではない。`config/slide-pages.json`に登録されていない限り、通常のHTMLとして直接編集する。

### スライド形式の共通仕様

- 対象ページの`body`に`data-lesson-slide-deck`、各スライドに`data-lesson-slide`と`data-lesson-slide-title`を設定する。DR固有のグラフや問題UIは`css/digital-representation.css`と`js/sound-*.js`へ残す。
- 前後ボタンと現在位置を含むスライド選択メニューは、共通基盤が学校名ヘッダーの直下へ生成する。配置と操作の詳細は`docs/lesson-slide-deck.md`を参照する。最後のスライドから次教材へ進む場合は、原則として`js/pages.js`の`next`を使い、HTMLへURLを重複記述しない。
- 見出しIDを含むURLのハッシュから、該当スライドを直接開ける状態を保つ。
- 全画面・最大化時におおむね1画面へ収めつつ、MacBook Airの画面を左右半分にした幅や390px幅でも操作できるようにする。内容が収まらない場合は、ページ全体ではなく表示中のスライド内をスクロールさせ、横長SVGには必要に応じて横スクロールを用意する。
- JavaScriptが動かない場合も本文を上から読める構造を保ち、次教材への通常リンクには`lesson-slide-fallback-next`を付けてHTMLに残す。スライド初期化後だけ、重複する次ページ案内やフッターを隠す。
- スライド移動で見出しへフォーカスできること、左右キー・Page Up・Page Downで移動できること、フォーム操作中にはキー移動を奪わないことを維持する。

### デジタルとアナログ・データ量（`dr01`）

- `dr01.html`は、`/Users/takashi/授業スライド/情報_授業スライド_03デジタル.pptm`の3・4・6枚目を基準にしたHTML教材とする。連続値・離散値、アナログ・デジタル、デジタル情報の特徴、通り数、必要なビット数、データ量の順で扱う。原本5枚目の基数変換は`dr02`へ分け、PPTM自体は変更しない。
- 本文は下表の10枚とし、共通基盤が生成するタイトル画面を0枚目に置く。`config/slide-pages.json`の生成対象には登録しない。

| 本文スライド | 内容 |
| --- | --- |
| 1 | 連続値と離散値：長さと玉の個数の比較 |
| 2 | アナログとデジタル：長さをそろえる間隔の操作 |
| 3 | デジタル情報の特徴：0・1での表現、メリットとデメリット |
| 4 | ビット数と情報の通り数：1〜8bitの操作と全組合せ |
| 5 | 必要なビット数：必要な通り数と2の累乗の比較 |
| 6 | バイトと単位換算：bit〜TB、1024倍・1000倍の換算 |
| 7 | 語句・ポイントまとめ：クリックして説明を開く |
| 8 | 考え方の確認問題：選択式3問 |
| 9 | 通り数・ビット数の問題：数値入力5問 |
| 10 | 単位換算の問題：数値入力5問 |

- 図は独自のインラインSVGとする。長さの操作は0〜10cm、0.01cm刻みとし、連続量を説明するためのモデルであることを明記する。2・1・0.5・0.1cm間隔の最も近い値へそろえ、中間ちょうどは大きい側へそろえる。量子化の専門的な説明や誤差の分類をここへ追加しない。
- ビットの組合せは各桁の切り替えと一覧から選べる。選択状態はフォーカスを保って更新し、一覧の矢印キーとスライダーのキー操作がスライド移動へ流れないようにする。必要な通り数は2〜65,536の整数を入力し、128通りと129通りの境界も確かめる。
- 例題・演習は断りがなければ1024倍換算とし、K・M・G・Tまで扱う。1000倍換算への切り替えは換算操作欄だけに適用し、ほかのスライドの問題条件は変えない。換算の途中では丸めず、操作欄で結果の表示を丸める場合だけ`≈`と有効数字12桁の注記を付ける。
- 原本の8bit＝256通り、120通りには7bit、2B＝65,536通り、500通りには9bit、4GB＝4096MB、4096×3072bit＝1.5MB、2048×2048B＝4MB、1MB＝2²⁰Bを踏襲する。2KBと1KBの通り数の比は2⁸¹⁹²倍とし、入力欄では指数8192を答えさせる。1000倍換算の比較問題は条件を明示して加える。
- 問題は13問を固定で用意し、種類ごとの選択欄で切り替える。数値入力は全角数字と正しい3桁区切りのカンマに対応する。解説は`details`を開いて「次へ」で途中の考え方を1段ずつ表示する。JavaScript無効時は本文・全問題・解説を通常の文書順に読める状態を保つ。
- 計算は`js/digital-basics-core.js`、操作は`js/digital-basics-lessons.js`、固有の表示は`css/digital-basics.css`で管理する。共通スライド操作やヘッダーメニューを複製しない。
- 2026-09-13に「ページ一覧」へ`dr01`を`release: false`で登録した。既存行の書式・入力規則を引き継ぎ、原本の読み戻しとGASの生成プレビューで追加差分が`dr01`だけであることを確認して、正式な`exportData_to_pages_js`で生成した（GASのコミット`aeff5c2`）。リンク公開と未作成の`dr02`への接続は未設定とする。
- 同日、計算・入力の144件、共通スライド構造の512件と、既存の論理回路アプリ・音ページ・リンク・検索・スライド生成の検証に合格した。ChromeとPlaywright WebKitで全10枚、全13問、1440・720・390px幅、全テーマ・文字サイズ、キーボード・タッチ、タイトル画面、メニューの排他制御、補足dialogのEscapeとフォーカス復帰、Fullscreen APIでの開始・ボタンでの終了、スライド内スクロールを確認した。390px幅の図中文字を拡大し、長さスライダーに`tabindex="0"`を付けてWebKitでもShift+Tabで戻れるようにした。ページ内例外と資産の404はなかった。印刷用表示とJavaScript無効時の本文・問題順も確認した。Safariアプリ自体、実画面でのEscによる全画面終了、GSSのネイティブ画面はMacがロックされていたため未確認。

### 2進数と16進数（`dr02`）

- `dr02.html`は、`/Users/takashi/授業スライド/情報_授業スライド_03デジタル.pptm`の5枚目と8〜12枚目を基準にしたHTML教材とする。原稿7枚目は単元のタイトル。原稿の20問を引き継ぎ、16進数から2進数へ戻す確認として`C4`と`0F`の2問を加える。負数・2の補数は`dr11`、小数・実数の表現は`dr12`へ分ける。
- 本文は次の11枚とし、共通基盤が0枚目のタイトル画面を生成する。Googleスライド生成フローの対象にはしない。

| 本文スライド | 内容 |
| --- | --- |
| 1 | 基数と位取り：同じ値を3種類の基数で表示し、繰り上がりを確認 |
| 2 | 10進数から2進数：37を2で繰り返し割り、余りを逆順に読む |
| 3 | 2進数から10進数：各桁の0・1を切り替え、位の値と合計を確認 |
| 4 | 16進数の数字：0〜Fと4bitの対応、Fから10への繰り上がり |
| 5 | 2進数から16進数：右から4桁ずつ区切り、左端だけ0を補完 |
| 6 | 16進数から2進数：各桁を必ず4bitにして並べる |
| 7 | 16進数と10進数：位の値による合計と16で割る方法を切り替え |
| 8 | 語句・ポイントまとめ：クリックして説明を開く |
| 9 | 10進数と2進数の問題：原稿の10問 |
| 10 | 2進数と16進数の問題：原稿2問と逆向きの確認2問 |
| 11 | 10進数と16進数の問題：原稿の8問 |

- 数値は原稿の`37`と`100101`、`1011011`と`5B`、`C4`と`11000100`・`196`を説明の基本にする。原稿の「ブロックごとを1桁の10進数に変換する」は、値10〜15が10進数では2桁になるため使用しない。4bitのグループを0〜15の値に対応させ、16進数1桁へ置き換える説明にする。
- 操作は0〜65,535の整数を扱い、2進数は最大16桁、16進数は最大4桁、10進数は最大5桁とする。1枚目の繰り上がり確認は0〜32。入力は全角数字・英字と16進数の小文字、2進数・16進数の桁を区切る空白、10進数の正しい3桁区切りのカンマに対応する。負号・小数・基数にない数字・範囲外を受理しない。
- 除算は「次の割り算」で1行ずつ追加し、商0まで進んだら余りを下から読む答えを表示する。0は1行で0と表示する。位取り表の桁ボタンは同じ桁数の間は作り直さず、フォーカスを保つ。幅広の位取り表・4bit対応表は表の枠内だけを横スクロールさせる。
- 2進数から16進数への変換では、補った0に下線を付け、補った個数も文章で示す。16進数から2進数への変換は先に各桁を4bitへそろえ、並べた数全体の先頭にある0だけを省略可能とする。`101`の操作例で途中の0を保持する意味も確認する。
- 計算は`js/digital-radix-core.js`、操作は`js/digital-radix-lessons.js`、固有表示は`css/digital-radix.css`で管理する。演習の問題選択・判定・段階解説は`js/digital-basics-lessons.js`を共用し、`data-db-source`・`data-db-source-base`・`data-db-answer-base`を持つ問題だけ基数変換の判定へ切り替える。`dr01`の既存問題は従来の計算を保つ。
- JavaScript無効時は静的な例と22問を文書順に表示し、`details`で解説を読めるようにする。7枚目の両方向の説明も両方表示する。
- 2026-09-13に「ページ一覧」へ`release: false`で登録し、既存の書式・入力規則を引き継いだ。`dr01.next=dr02`・`dr02.back=dr01`を原本へ設定し、未作成の`dr11`への接続とリンク掲載は保留する。正式生成前の差分が`dr02`の追加と`dr01.next`だけであることを確認し、`exportData_to_pages_js`で生成した（GASのコミット`c9874e5`）。
- 同日、基数変換・入力の107件と共通スライド構造の519件、既存の論理回路アプリ・音ページ・リンク・検索・スライド生成の検証に合格した。原稿20問と追加2問の答え・段階解説も照合した。ChromeとPlaywright WebKitで全11枚・22問、1440・720・390px幅、全テーマ・文字サイズ、空欄・不正入力・誤答・全角・小文字・空白区切りの採点、解説の送りとリセット、0と65,535の変換、補完する0と途中の0、キーボード・タッチ、タイトル画面、メニュー排他制御、検索dialogのEscapeとフォーカス復帰、Fullscreen APIでの開始・ボタンでの終了、スライド内スクロールを確認した。ページ内例外と資産の404はなかった。印刷用表示とJavaScript無効時の11枚・22問・両方向の説明も確認した。共用する`dr01`の13問と既存操作も両ブラウザで回帰確認した。Safariアプリ自体、実画面でのEscによる全画面終了、GSSのネイティブ画面はMacがロックされていたため未確認。

### 数値のデジタル表現・整数（`dr11`）

- `dr11.html`は、`/Users/takashi/授業スライド/情報_授業スライド_03デジタル.pptm`の13〜17枚目を中心に、24〜25枚目の加減算も参照したHTML教材とする。2026-09-13にユーザーが承認したタイトル1枚＋本文11枚、演習12問の構成を保つ。タイトルは共通基盤が生成し、Googleスライド生成フローへは登録しない。

| 本文スライド | 内容 |
| --- | --- |
| 1 | 整数とビット数：4bit・8bitで先頭へ0を補う |
| 2 | 負の数と符号ビット：4bitの桁を切り替え、符号付き・符号なしを読み比べる |
| 3 | 2の補数の求め方：4bitの6を反転し、1を加えて−6にする |
| 4 | 8bitで負の数を表す：37を反転し、1を加えて−37にする |
| 5 | ビット列から整数を読み取る：正負を判別し、負なら絶対値を求める |
| 6 | 2の補数と加減算：6＋(−6)、5−3と自由入力 |
| 7 | 表せる整数の範囲：4bit・6bit・8bitの最小値と最大値、4bit全値・8bit代表値の一覧 |
| 8 | 表現範囲の境界：4bitの7＋1と、幅を変えた比較 |
| 9 | 語句・ポイントまとめ：クリックして説明を開く |
| 10 | 2の補数の演習：原稿の8問、元のビット列・1の補数・2の補数を記入 |
| 11 | 読み取り・範囲の演習：読み取り2問、8bitの範囲1問、4bitで＋8を表せるか1問 |

- 原稿の演習は4bitの2・4・5・7、8bitの50・85・127・17を引き継ぐ。追加の読み取りは4bitの`1010`（−6）と8bitの`11011011`（−37）。各設問の解説は共通の「次へ」で1段ずつ示す。
- 符号付き整数は2の補数による表現に限定する。符号ビットだけを変える方法とは区別し、`0110`の先頭だけを変えた`1110`は−2であることを説明する。読み戻しで得た絶対値のビット列は符号なしとして読む。最小値`1000`の絶対値を8と読んで−8に戻す例を添える。
- 原稿の4bit範囲表にある−2・＋1のビット列は、それぞれ`1110`・`0001`とする。6bit欄の不整合は承認済みの修正方針に従い、−32〜31にそろえる。4bitは−8〜7、8bitは−128〜127。PPTM自体は変更しない。
- 7枚目の「整数の一覧」は原稿16枚目の表に対応する。4bitは＋7から−8までの全16値、8bitは＋127〜＋124、＋4〜−4、−124〜−128の18値を10進数の降順で載せ、省略箇所を示す。最大値・最小値を明記し、0と−1の間を太い罫線で区切る。原稿同様、＋8・＋128がそれぞれ表現不可であることも添える。「範囲と操作」との切り替えは座学共通基盤の`data-lesson-view-group`を使い、一覧への直リンクは`#di-integer-values`とする。本文は11枚のまま、JavaScript無効時と印刷時は両方の表示内容を読むことができる。
- 一覧追加時に、34行すべての値・ビット列をPPTM原本と照合し、符号付き2進数としての値も検算した。Chrome・Playwright WebKitで各27通り（1440・720・390px、全テーマ・文字サイズ）の表示、切り替えのEnter・矢印・End・Tab・Shift+Tab・タッチ、直リンク、前後のスライド移動、範囲操作の入力状態保持、スライド内スクロール、JavaScript無効時・印刷時の両表表示を確認した。共通スライド構造526件・論理回路アプリ・音ページ・スライド生成の検証にも合格した。Safariアプリ自体は未確認。
- 一覧表のスクロール領域には`data-lesson-slide-navigation-lock`を付け、フォーカス中の矢印キーを表の横スクロールに使えるようにする。
- 加減算は、結果の下位のビットだけを残す模型として説明する。幅外への繰り上がりと符号付き整数のオーバーフローは別々に判定する。`6＋(−6)`は繰り上がりあり・範囲内、`7＋1`は繰り上がりなし・オーバーフロー。数学上の答えと残したビット列の値を併記し、範囲外の値を正しい答えとして扱わない。詳しい演算誤差は`dr13`へつなぐ。
- 計算は`js/digital-integer-core.js`、操作は`js/digital-integer-lessons.js`、固有の表示は`css/digital-integer.css`へ置く。スライド移動・全画面・問題選択・段階解説は既存基盤を共用する。`js/digital-basics-lessons.js`の`data-db-answer-part`を持つ入力だけ、固定桁数のビット列または符号付き10進整数として複数欄を個別採点する。既存の`dr01`・`dr02`は従来の採点分岐を保つ。
- 入力は全角数字、Unicodeの負号、ビット列の空白区切りに対応する。演習のビット列は指定された4桁・8桁を厳密に要求し、先頭0の省略を受理しない。補数を作る操作の0は幅外の繰り上がりを除いて0に戻す。加減算は最小値の減算も扱う。無効な入力では結果を隠し、印刷用表示でも古い結果を出さない。
- 2026-09-13に「ページ一覧」へ`release: false`で登録した。既存行の書式・入力規則を引き継ぎ、`dr02.next=dr11`・`dr11.back=dr02`を設定した。原本を読み戻し、生成差分が新規行と`dr02.next`だけであることを確認して、正式な`exportData_to_pages_js`で生成した（GASのコミット`c5b5598`）。未作成の`dr12`への接続と一覧掲載は保留する。
- 同日、整数計算・入力の89件と共通スライド構造の526件、既存の論理回路アプリ・音ページ・リンク・検索・スライド生成の検証に合格した。ChromeとPlaywright WebKitでタイトルと本文11枚・演習12問、1440・720・390px幅の全テーマ・文字サイズ、全角・空白区切り・負数・不正入力の採点、0と最小値、繰り上がりとオーバーフロー、段階表示とリセット、キーボード・タッチ、メニュー排他制御、検索dialogのEscapeとフォーカス復帰、Fullscreen APIでの開始・ボタンでの終了、スライド内スクロールを確認した。ページ内例外と資産の404はなかった。JavaScript無効時と印刷用表示の本文11枚・演習12問・補数の全手順も確認した。共用する`dr01`の13問・`dr02`の22問と既存操作は両ブラウザで回帰確認した。Safariアプリ自体、実画面でのEscによる全画面終了、GSSのネイティブ画面は未確認。

### 数値のデジタル表現・実数（`dr12`）

- `dr12.html`は、`/Users/takashi/授業スライド/情報_授業スライド_03デジタル.pptm`の18〜22枚目を基準にしたHTML教材とする。2026-09-13に承認されたタイトル1枚＋本文12枚、演習8問の構成。タイトル・スライド操作は座学共通基盤を使い、Googleスライド生成フローへは登録しない。PPTM原本は変更しない。
- 基数は2026-09-13のユーザー指定により、数値の後ろへ丸括弧付きで下付き表示する（`<sub>(2)</sub>`・`<sub>(10)</sub>`）。本文・表・演習の解説と、操作で更新する結果表示をそろえる。`dr02`の既存表記と同じ形式を使う。

| 本文スライド | 内容 |
| --- | --- |
| 1 | 2進数の小数：1/2・1/4・1/8・1/16の対応表とビット切り替え |
| 2 | 小数の変換：0.75・6.75を、2倍して整数部を取り出す手順で示す |
| 3 | 固定小数点数：符号なし8bit、整数部4bit・小数部4bitで5.3125・6.75を表す |
| 4 | 固定小数点の範囲と刻み：小数部2・4・6bitを比較する |
| 5 | 浮動小数点と正規化：小数点を動かし、仮数と指数に分ける |
| 6 | 32bit形式：符号部1bit・指数部8bit・仮数部23bit、先頭の1の省略 |
| 7 | 指数とバイアス：指数に127を加え、読み戻すときは127を引く |
| 8 | 5.75・−5.75：2進変換、正規化、符号部、指数部、仮数部の順に表示 |
| 9 | 0.025：循環する2進小数と、23bitでの切り捨て・最近接丸めの比較 |
| 10 | 16bit形式：3.125、符号部1bit・指数部5bit・仮数部10bit、バイアス15 |
| 11 | 語句・ポイントまとめ：語句や問いごとに説明を開く |
| 12 | 問題演習：固定小数点3問、正規化2問、32bit形式2問、16bit形式1問 |

- 固定小数点は原本どおり符号なしの4bit＋4bitから導入し、符号付き固定小数点は扱わない。`0101.0101₂=5.3125`、`6.75=0110.1100₂`、範囲0〜15.9375、刻み1/16を使う。8bitの割り当てを変える操作では、組合せ256通りを保ったまま範囲と刻みが変わることを示す。表せない値を入力したときは丸めて代入せず、結果を隠して入力条件を示す。
- 正規化では指数の正負と小数点の移動方向を対応させる。浮動小数点の負数は符号部で扱い、整数で学んだ2の補数を求めない。0には専用の表示を設け、「1.……」へ正規化したりバイアスを加えたりしない。入力の`-0`はこの教材では`+0`にそろえる。
- 原本20枚目の5.75は、符号部`0`・指数部`10000001`・仮数部`01110000000000000000000`。22枚目の3.125は、16bit形式で`0`・`10000`・`1001000000`。演習6〜8も同じ条件とし、各欄の先頭0を含めて指定桁数を要求する。
- 原本21枚目の0.025は23bitで切り捨てる例で、仮数部は`10011001100110011001100`。最近接丸めでは末尾が1の`10011001100110011001101`になる。両方を区別して載せ、10進数へ戻した表示値には近似値であることを明記する。通常の丸めの根拠は[Oracle Numerical Computation Guide / IEEE Arithmetic](https://docs.oracle.com/cd/E19059-01/stud.10/819-0499/ncg_math.html)で確認した。演算誤差の分類・影響・対策は`dr13`へ分ける。
- 計算は`js/digital-real-core.js`、操作は`js/digital-real-lessons.js`、表示は`css/digital-real.css`へ置く。10進入力を内部ではBigIntの分数として扱い、2倍法と16/32bitへの丸めを正確に求める。最近接・偶数丸めと0方向への切り捨てを区別する。操作UIの自由入力は通常の正規化数と0に絞り、特殊値を学習範囲へ加えない。32bitは絶対値0.000001〜1,000,000、16bitは0.0001〜65,504を扱う。
- 演習の問題選択、採点、段階解説は`js/digital-basics-lessons.js`を共用する。`data-db-answer-part="decimal"`を追加し、ビット列の指定幅を1〜32bitへ対応させた。実数コアを読み込まない`dr11`は既存の整数コアを使用し、`dr01`・`dr02`の採点分岐を保つ。全角数字、小数点、Unicodeの負号、ビット列の空白区切りに対応する。
- 2026-09-13に「ページ一覧」へ`release: false`で登録し、`dr11.next=dr12`・`dr12.back=dr11`を設定した。書式と入力規則を引き継ぎ、新規行・前ページのリンク・周囲の既存行を読み戻して確認した。正式生成前の差分は新規行と`dr11.next`だけで、既存警告6件の増加はない。`exportData_to_pages_js`で生成した（GASのコミット`38c9bad`）。検索索引は71ページのまま変更なし。未作成の`dr13`への接続と一覧掲載は保留する。
- 計算コア77件、共通スライド構造533件、既存の論理回路アプリ・音ページ・検索・スライド生成の検証に合格した。ChromeとPlaywright WebKitで、全13枚・演習8問、1440・720・390px幅の全テーマ・文字サイズ（各324通り）、全角・負数・空白・不正入力の採点、0・範囲外・丸めによる指数の繰り上がり、段階表示・リセット・キーボード・タッチ、メニュー排他制御・検索dialogのEscapeとフォーカス復帰、全画面開始と終了ボタン、スライド内スクロールを確認した。ページ内例外と資産の404はない。JavaScript無効時と印刷用表示でも本文12枚・演習8問・変換手順を読める。Safariアプリ自体、実画面でのEscによる全画面終了、GSSのネイティブ画面は未確認。

- 共通採点処理の拡張後、`dr01`の13問・`dr02`の22問・`dr11`の12問と既存操作をChrome・Playwright WebKitで回帰確認した。`dr02`の画面幅変更直後の検証はレイアウト更新完了を待って実施し、教材側の変更は行っていない。

### 演算誤差（`dr13`）

- `dr13.html`は、`/Users/takashi/授業スライド/情報_授業スライド_03デジタル.pptm`の27〜28枚目を中心に、24枚目のオーバーフロー例も参照したHTML教材とする。2026-09-13に承認されたタイトル1枚＋本文11枚、演習8問の構成。表紙とスライド操作は座学共通基盤で生成し、PPTM原本は変更しない。

| 本文スライド | 内容 |
| --- | --- |
| 1 | 有限の桁数・範囲と、計算を終える時点から誤差の原因を考える |
| 2 | 丸め誤差：0.3の2進小数を4・8・12・16bitで保存し、切り捨てと最近接丸めを比較する |
| 3 | 打ち切り誤差：1/2＋1/4＋…を1〜16項で終え、1までの残りを比較する |
| 4 | 情報落ち：1.234＋1.234×10⁻⁴を起点に、指数と有効数字を変える |
| 5 | 桁落ち：有効数字6桁の近似値0.0316069と0.0315753の差を考える |
| 6 | 情報落ちと桁落ち：数の関係、計算、失われる情報を比較する |
| 7 | オーバーフロー：符号付き8bitで70＋63、負の側の限界も確かめる |
| 8 | アンダーフロー：16bit浮動小数点数で0に近い数の精度と0への丸めを見る |
| 9 | 工夫：保存精度、計算順、計算回数。1と0.0001を加える順序を比較する |
| 10 | 語句・ポイントまとめ：説明を個別に開く |
| 11 | 問題演習：計算3問、現象・条件・対策の選択5問、段階解説 |

- 0.3の2進小数部8bit切り捨ては`0.01001100`（0.296875）、差は0.003125。基数の下付きはユーザー指定の`<sub>(2)</sub>`・`<sub>(10)</sub>`を、静的本文と動的表示で共用する。最近接丸めは同距離なら末尾ビットが0の方を選び、1への繰り上がりにも対応する。
- 原本の「桁を捨てる」例と区別するため、打ち切り誤差にはユーザー承認済みの無限等比級数の例を使用する。各項・有限和は正確に計算し、最初の4項では15/16＝0.9375、残り1/16＝0.0625。丸めで桁を捨てることと、計算を有限回で終えることを混同しない。
- 情報落ち・計算順の操作は、10進数の有効数字4・6・8桁で各計算後に切り捨てる学習用モデル。小数点以下の桁数やブラウザの標準浮動小数点演算とは区別する。0.0001を10・100・1000個加える例では、小さい数をまとめる順序と1から加える順序を比較する。
- 桁落ちの入力は有効数字6桁に丸めた近似値であると明記する。表示値どうしの差0.0000316へ末尾0を補っても精度は増えない。各入力の四捨五入誤差を最大±0.00000005とした補足では、差のずれの上限を±0.0000001とする。正確な値どうしの引き算が必ず新たな誤差を生むとは説明しない。[Goldberg / What Every Computer Scientist Should Know About Floating-Point Arithmetic](https://docs.oracle.com/cd/E19957-01/806-3568/ncg_goldberg.html)で近似値の桁落ちと丸めの関係を確認した。
- 符号付き8bitの70＋63は133で範囲外。下位8bitだけを保持した`10000101`の解釈は−123であり、133と混同しない。負の側の−128−1もオーバーフローである。実際の範囲外処理は形式・言語によって異なるため、下位8bitを保持する説明はモデルに限定する。
- アンダーフローは絶対値が0に近い数の精度として扱う。16bit（符号1・指数5・仮数10）の`1.0000000001`（2進数）を2で12〜26回割った数学的な値を、その都度最近接・偶数丸めで保存する。保存値を次の割り算の入力にする反復計算ではない。14回では正規化数で正確、15回では非正規化数で丸め誤差、24・25回では最小の正の値2⁻²⁴、26回では0となる。10進表示を有効数字5桁の近似と明記し、正確なビット列を併記する。非正規化数をすべて0・不正確とは説明せず、追加の専門語は開閉式の補足へ置く。[Oracle Numerical Computation Guide / IEEE Arithmetic](https://docs.oracle.com/cd/E19059-01/stud.10/819-0499/ncg_math.html)のgradual underflowに基づく。
- 計算は`js/digital-errors-core.js`のBigInt整数比、操作は`js/digital-errors-lessons.js`、専用表示は`css/digital-errors.css`。演習の問題選択・採点・段階解説は既存の`js/digital-basics-lessons.js`と実数入力処理を共用し、共通処理の変更はない。入力条件に合わない場合は結果を隠し、入力欄ごとに不正状態を示す。JavaScript無効時・印刷時も問題の選択肢を文章として残す。
- 2026-09-13に「ページ一覧」へ`release: false`で追加し、`dr12.next=dr13`・`dr13.back=dr12`を設定した。既存行の書式と入力規則を引き継ぎ、追加行・前ページ・周囲の既存行をCellDataで読み戻して確認した。生成差分は追加行と`dr12.next`だけで、既存警告6件の増加はない。正式な`exportData_to_pages_js`で生成した（GASのコミット`aa879db`）。検索索引は71ページのまま変更なし。`dr21`への接続と一覧掲載は保留する。
- 計算コアの原本例・境界値、構文、共通スライド構造540件、論理回路アプリ、音ページ324件、スライド生成、検索の検証に合格した。ChromeとPlaywright WebKitで全12枚・演習8問、1440・720・390px幅と全テーマ・文字サイズ（各324通り）、全角・不正入力、丸めの繰り上がり、正負の限界、計算順、個別採点と段階解説、キーボード・タッチ、検索dialogのEscapeとフォーカス復帰、メニュー排他制御、Fullscreen APIでの開始・ボタンでの終了、内部スクロールを確認した。WebKitの文字拡大時に選択欄内部がはみ出す問題は専用CSSで修正し、44px以上の操作領域も確認した。ページ内例外・資産の404はない。印刷用表示とJavaScript無効時の本文・全8問・選択肢も確認した。Safariアプリ自体、実画面のEscによる全画面終了、GSSのネイティブ画面は未確認。

### 文字のデジタル表現（`dr21`）

- `dr21.html`は、`/Users/takashi/授業スライド/情報_授業スライド_03デジタル.pptm`の34〜36枚目を参照したHTML教材。2026-09-13に承認されたタイトル1枚＋本文11枚、演習8問の構成とする。PPTM原本は変更しない。表紙・前後移動・現在位置・全画面は座学共通基盤を使用する。

| 本文スライド | 内容 |
| --- | --- |
| 1 | 文字に番号を付ける。数値の1と文字「1」の違い |
| 2 | ASCIIの7bit・128通りと、1Bでの保存、制御文字 |
| 3 | 全128項目のASCIIコード表。上位4bit・下位4bit、行・列と選択文字の連動 |
| 4 | 原本のKaijoを1文字ずつ符号化する段階表示 |
| 5 | ビット列から文字を読む。文字列・2進数・16進数の相互変換 |
| 6 | 日本語の文字集合・符号化方式。JIS、Shift_JIS、ISO-2022-JP |
| 7 | Unicodeとコードポイント。文字の番号と保存の方式を分ける |
| 8 | UTF-8の1〜4Bとデータ量。入力文字列の各コードポイントと合計 |
| 9 | 「日本」を3方式で保存し、3方式で読み比べる文字化け例 |
| 10 | 語句・ポイントまとめ。説明を個別に開く |
| 11 | 演習8問。ASCII符号化・復号、文字の0、通り数、Unicode・UTF-8、データ量、文字化け |

- 原本の`Kaijo`は`4B 61 69 6A 6F`、`JohO`は`4A 6F 68 4F`、復号問題は`4A 49 53 63 6F 64 65`→`JIScode`を踏襲する。ASCIIの範囲は00〜7F、5Cはバックスラッシュ、7Eはチルダ。元資料の表記だけを根拠にJIS全般と同一視しない。NULは空の文字列との混同を避けて「ヌル（値0）」とする。
- 原本のUnicode「2〜4バイト」・UTF-8「1〜6バイト」は、ユーザーが承認した構成に従い、UnicodeのコードポイントとUTF-8の符号化を分け、UTF-8は1コードポイント1〜4Bとする。[Unicode FAQ](https://www.unicode.org/faq/utf_bom.html)・[RFC 3629](https://www.rfc-editor.org/rfc/rfc3629)・[RFC 20](https://www.rfc-editor.org/rfc/rfc20)で確認した。見た目の1文字が複数コードポイントになる場合は開閉式の補足に置き、UTF-16のサロゲートやBOMの説明は広げない。
- JISは文字集合なども定める規格、Shift_JISとISO-2022-JPは符号化方式として扱う。「JISコード」と呼ばれることがあるISO-2022-JPだけをJISの定義にしない。「日本」はShift_JISで`93 FA 96 7B`、UTF-8で`E6 97 A5 E6 9C AC`、ISO-2022-JPで`1B 24 42 46 7C 4B 5C 1B 28 42`（状態切り替えとASCIIへの復帰を含む）。例のバイト列はPythonの各codecで照合した。文字化けの読み取りはブラウザの`TextDecoder`を使用し、不正な列を別の文字列として捏造せず、読み取れない場合を区別する。
- 変換処理は`js/digital-text-core.js`、操作は`js/digital-text-lessons.js`、専用表示は`css/digital-text.css`へ置く。UTF-8の文字入力は空白を削除せず、Unicode正規化もしない。上限は32コードポイントで、見た目の文字数とは区別する。バイト列の入力だけは全角英数字・空白区切りに対応し、2進数8桁・16進数2桁の区切りを守る。画面への入力値はテキストノードで表示する。
- ASCII表は静的HTMLにも全128項目を載せる。表内のTab停止は選択中の1項目に限定し、矢印キー・Home・Endで移動する。横・縦スクロールと行列の見出し固定には専用の`x-scroll`ラッパを使い、`script_pages.js`による追加の横スクロールラッパを避ける。共通ナビゲーションやスクロール処理は変更しない。
- 問題選択・採点・段階解説は`js/digital-basics-lessons.js`を共用する。今回追加した`bytes`型はバイト列を比較し、`text`型は半角英字の大文字・小文字を区別する。`text`型の採点では入力両端の余白を無視するため、空白・制御文字自体を正答にする問題へ流用しない。既存の数値・ビット列採点は保持する。JavaScript無効時・印刷時にも本文、ASCII表、全8問、3問分の選択肢を残す。
- 2026-09-13に「ページ一覧」の17行目へ`release: false`で追加し、`dr13.next=dr21`・`dr21.back=dr13`を設定した。入力規則（releaseはTRUE/FALSE、contentTypeはlecture/practice）・書式・近接する5既存行をCellDataで確認した。正式生成前の差分は追加行と`dr13.next`だけで、既存警告6件は増えていない。`exportData_to_pages_js`で正式生成した（GASのコミット`1cf52e4`）。検索索引は71ページのまま。`dr22`への接続と一覧掲載は保留する。
- コア686件、構文、共通スライド構造547件、論理回路アプリ、音ページ324件、スライド生成・内容検証、検索の検証に合格した。ChromeとPlaywright WebKitで全12枚・演習8問、1440・720・390px幅×全テーマ×全文字サイズ（各324通り）、ASCII全128項目の選択、文字・バイト列・空白・改行・全角・絵文字・結合文字・不正入力、文字化け9通り、各問の正誤・入力リセット・段階解説を確認した。コード表の行列見出し固定と端へのスクロールは各27条件で確認し、キーボード・タッチ・検索dialogのEscapeとフォーカス復帰・メニュー排他制御・Fullscreen APIでの開始とボタンでの終了も確認した。採点共通処理はChromeでdr01〜dr13の既存63問を回帰確認済み。ページ内例外・資産の404はない。JavaScript無効時と印刷用表示の本文・全問題・選択肢も確認した。Safariアプリ実機、実画面でのEscによる全画面終了、GSSのネイティブ画面は未確認（GSSはCellDataで書式を確認）。

### 色のデジタル表現（`dr22`）

- 2026-09-13に、ユーザー承認済みの構成で初稿を作成した。原本は `/Users/takashi/授業スライド/情報_授業スライド_03デジタル.pptm` の37〜39枚目。原本のPPTMは変更しない。共通スライド基盤が表紙を生成し、本文11枚と合わせて全12枚とする。

| 本文スライド | 内容 |
| --- | --- |
| 1 | 色を見るしくみ。3種類の錐体の反応を組み合わせることとRGBへの導入 |
| 2 | 光の三原色と加法混色。RGBの出す／出さないを切り替える図 |
| 3 | 色の三原色と減法混色。CMYの吸収を切り替える図と印刷のCMYK |
| 4 | RGB各0〜255・256段階。スライダーと数値入力、色見本 |
| 5 | 基本8色と灰色3段階（64・128・192）のRGB表 |
| 6 | RGB各8bit、合計24bit、16,777,216色の段階式の説明 |
| 7 | 各1・2・4・8bitの段階数・合計bit数・色数の表とグラデーション |
| 8 | #RRGGBBと、RGB(47,82,130)を#2F5282へ変換する段階説明 |
| 9 | RGB・カラーコードの相互変換。各成分の10進数・2進数・16進数と色見本 |
| 10 | 語句・ポイントまとめ。説明を個別に開く |
| 11 | 演習8問。混色、無彩色、色数、4,096色、RGBとカラーコード、近い色名 |

- 原本38枚目の一部の `RGB(47,82,139)` は、他の記述とカラーコード `#2F5282` に合わせ、承認済みのとおりBを130へ統一する。139なら `#2F528B` になる。原本39枚目の4,096色→各4bit・16段階、`RGB(114,199,111)`→`#72C76F`、`#CA3127`→`RGB(202,49,39)`、4つの近い色名の問題を踏襲する。`#008800` はRGB(0,136,0)で、指定の選択肢では緑とする。
- 錐体はL・M・Sの反応範囲が重なることを明記し、RGBだけを個別に検出するセンサーとして描かない。原本にある第三者の眼球図は転載しない。CMYの図は白い光のR・G・Bを各色材が吸収する理想化した説明用モデルであり、実際のインクの再現やRGBからCMYKへの数値変換には使わない。印刷用のKを紹介する範囲にとどめる。
- 変換・量子化・混色の計算は `js/digital-color-core.js`、操作は `js/digital-color-lessons.js`、専用表示は `css/digital-color.css` に置く。混色図は独自SVGの7領域を計算して塗り分け、テーマやCSSの混合モードによって色が変わらないようにする。色だけに頼らず、RGB値、色名、残る光の成分も併記する。
- グラデーションはRGBのコード値を等間隔に区切る模型。物理的な光量や知覚上の明るさが等間隔だとは説明しない。1色を表すビット数と色数を扱い、画像全体のデータ量は `dr41` に置く。
- 変換入力はRGB各0〜255の整数、カラーコードは任意の先頭#と16進数6桁に限定する。小文字と全角英数字は受け付けるが、3桁・8桁の省略形・透明度指定や不正な値は受け付けない。不正な入力では色見本・変換結果を隠し、入力欄と説明で知らせる。基数は数字の後に丸括弧付きの下付き `(2)`・`(10)`・`(16)` を用いる。
- 問題選択・採点・段階解説は `js/digital-basics-lessons.js` を共用する。追加した `color` 型はRGBの3成分へ読み取って正規化したカラーコードを比較する。色名の設問は日本語ラベルの選択欄とし、既存 `text` 型へASCIIの選択値を渡す。JavaScript無効時と印刷時にも本文・表・全8問・色名の選択肢を残す。
- 「配付ファイル一覧」の「ページ一覧」18行目へ `release:false`・`contentType:lecture` で追加し、`dr21.next=dr22`・`dr22.back=dr21` を設定した。17〜19行目のCellDataで内容、入力規則、書式、隣接行の保持を確認した。正式生成前の差分は追加行と `dr21.next` の2件だけで、既存警告6件は増えていない。`exportData_to_pages_js` によって正式生成した（GASのコミット `725009a`）。検索索引は71ページのまま。目次・検索への掲載は保留する。
- コア585件、共通スライド構造554件、音ページ324件、論理回路アプリ、スライド生成・内容検証、検索、JavaScript構文の検証に合格した。ChromeとPlaywright WebKitで各324条件（1440・720・390px幅×全テーマ×全文字サイズ×12枚）、混色16通り、4種類のビット数、RGB・カラーコードの正規化と不正入力、演習8問の正誤・リセット・段階解説を確認した。加えて各32条件で全問題の狭い画面表示、横スクロール表の右端、マウスドラッグ・タッチでのスライダー操作を確認した。キーボード、検索dialogのEscapeとフォーカス復帰、メニュー排他制御、Fullscreen APIによる開始とボタンでの終了、JavaScript無効時・印刷時の全本文と選択肢も確認した。ページ内例外・資産の404はない。共通採点はChromeで既存の `dr01`・`dr02`・`dr11`・`dr12`・`dr13`・`dr21` の計71問を回帰確認した。Safariアプリ実機、実画面のEscによる全画面終了、GSSのネイティブ画面は未確認（GSSの値・書式・入力規則はCellDataで確認）。

### 音のデジタル表現（`dr31`〜`dr32`）

- `dr31`と`dr32`はウェブ教材として個別に整備しているため、共通基盤やUIの保守では現行HTMLを正本とし、PPT・PDFとの照合を必須にしない。教材内容を原本に合わせる依頼が明示された場合だけ、指定された原本を確認する。
- 学習者向け本文はウェブページだけで完結させ、「授業資料を参照」「資料の例」のように外部資料の閲覧を前提とする表現を入れない。
- 原本にない専門用語を学習者向け表示へ追加しない。特に「エイリアシング」「ナイキスト周波数」は使わず、「元の波形とは異なる波形として見える」「同じ標本値から区別できない別の波形」など、ページ内で意味が分かる表現にする。内部の関数名まで変更する必要はない。
- PCMは「パルス符号変調」の略であり、標本化・量子化・符号化によって音をデジタルデータとして表す方式として説明する。原本にない仕組みは独自に付け足さない。
- `dr31`は音の概念説明を1ページへ統合する。最初にアナログ波形を示し、アナログとデジタル、標本化・量子化・符号化、条件変更、標本化定理、用語と数値の例の順に進み、最後に波形デジタル化問題を置く。
- `dr32`は「音のチャンネル」に続けて、「標本化周波数と標本化周期」「量子化ビット数と量子化段階数」「音声データ量」「用語と標本化定理」を各1スライドで練習する5スライド構成とする。問題UIにフォーカスがある間は、共通基盤の矢印キー移動を抑止する。
- `dr31`の最初のスライドでは、音源から空気の振動がマイクへ届き、電気信号へ変換される図をアナログ波形の前に表示する。音源・音波・マイクはインラインSVGで描き、図の説明をSVGのtitle・descとHTMLの説明文に置く。音源とマイクの図は幅320pxを上限に縦横比を保って中央へ置き、狭い画面では横スクロールする波形グラフの幅に配置を引っ張られないようにする。図と波形を1つの表示領域へまとめ、アナログ・デジタル・波形の意味と同じスライドで扱う。
- 音源の弧は4.8秒周期・1.2秒間隔で同じ速さでマイクへ進み、強弱を弧の太さ・濃さ・高さで示す。各弧と同じ遅延のマイク輪郭を到達時に短く強調する。これは空気の粒子や音速を再現した物理シミュレーションではなく、振動の伝わり方の模式表現であり、実際の音は出さない。「一時停止／再生」を図の近くへ置き、別スライド・非表示タブ・画面外では停止する。`prefers-reduced-motion`、JavaScript無効時、印刷時は静止図を保ち、波形グラフや読み上げをアニメーションへ連動させない。
- `dr31`の標本化・量子化・符号化は別スライドへ分割せず、同じSVGへ要素を累積表示する1スライドの教材とする。最初はアナログ波形だけを表示し、標本化では時間方向の区切り、量子化では段階と値、符号化では2進数を順に加える。
- `dr31`の固定例・条件変更グラフの量子化表示では、量子化幅1段分のブロックを段階値の個数だけ積み上げる。グラフ上の数字とブロック数は段階値を示し、電圧とは区別する。段階値0ではブロックを描かず数字0を表示する。ブロックを描画範囲内へクリッピングし、標本のホバー・フォーカスでは同じ標本のブロックも強調する。量子化前後の点と誤差表示、符号化時の2進数を保ち、工程の追加アニメーションは動きを減らす設定に従う。
- `dr31`の工程見出しは「0. アナログ波形」「1. 標本化」「2. 量子化」「3. 符号化」で統一する。内部状態を1〜4で保持していても、学習者向け番号へそのまま露出させない。
- `dr31`のPCM可変グラフでは位相を操作項目にしない。周波数の異なる2つの正弦波へ同じ位相角を加えると合成波の形が変わるため、波形全体の平行移動と混同しやすい。位相は同じページ内の波の重ね合わせで各成分を別々に確認し、将来PCM可変グラフで平行移動を扱う場合は位相角ではなく時間移動として設計する。
- `dr31`の可変グラフでは、0. アナログ波形の段階で標本化周波数と量子化ビット数を無効にし、1. 標本化では量子化ビット数だけを無効にする。標本化周期は標本化周波数の操作欄、量子化段階数は量子化ビット数の操作欄へまとめ、独立した計算カードを重複表示しない。
- `dr31`の条件変更スライドでは、初期表示を「0. アナログ波形」のみとする（`data-stage="1"`）。グラフの時間範囲を0〜1.2秒とし、操作欄には標本化周期と量子化段階数だけを表示する。標本数、量子化の幅、グラフ下の標本値表は、グラフと重複するため表示しない。選択した標本の値はグラフ直下の短い表示で確認できる状態を保つ。
- 工程の追加は理解を助ける短いフェード・ワイプで示し、`prefers-reduced-motion`ではアニメーションを停止する。補足は`i`、`?`、`!`のアイコンからホバー・フォーカス・タップで確認できるようにしてよいが、学習に必須の情報をアイコン内だけへ隠さない。
- 標本のホバー・フォーカス同期は対応する線と点を太くする程度に留め、標本区間全体を背景色で囲まない。ポインタまたはキーボードフォーカスがグラフ外へ出たときは強調を解除する。
- `dr31`の標本化定理は、標本化周波数が成分波の最大周波数の「2倍より大きい」「2倍ちょうど」「2倍より小さい」を区別する。2倍ちょうどは位相によって元の波形を一意に判断できない場合がある境界として示す。
- 正弦波を決める振幅・周波数・位相と波の重ね合わせは、標本化定理を理解するための予備知識として独立したスライドにせず、標本化定理のスライドから必要に応じて開く補足dialogに置く。
- 標本化定理の位相操作は、`fs = 2f`の境界で標本時刻と波の位置関係によって標本値が変わり、元の波を一意に判断できない場合があることを確かめるために維持する。通常の波形操作ではなく境界確認用であることを説明上明確にする。
- `dr31`の破線は「同じ標本点を通り得る別の波形」であり、復元された正解波形とは説明しない。標本点を折れ線で結んだものを復元波形として表示しない。
- `dr31`の標本化定理では、条件を変えた直後は元の波形と標本点だけを表示する。「標本点を結ぶグラフを描く」の操作後に、標本点を通る滑らかな波形を左から右へ表示する。条件を満たす場合は元の波形と重なる再現波形、境界または条件不足では元の波形と区別できない別の候補として扱う。重なった破線を見分けられるよう元の波形は再現波形の約2倍の太さにする。判定文など文字量が変わる要素はグラフ下へ置き、グラフ位置を動かさない。
- `dr31`の標本化定理の周波数スライダーは共通目盛を1〜20Hzとし、元の波は1〜10Hz、標本化周波数は2〜20Hzだけを選択可能にする。選択できない部分は灰色にし、色だけに頼らず有効範囲も文章で示す。計算値と波形描画ボタンはスライダー枠内にまとめる。
- `dr31`の標本化定理をスライド4、用語と数値の例をスライド5に置く。用語と数値例は左右2つのまとまりとして示し、各項目はクリック、タップ、キーボードで説明または途中式と答えを開けるようにする。用語には標本化定理を含める。
- 波形デジタル化問題は`dr31`の最終スライドに置き、標本単位の正誤と考え方を示す。
- `dr32`のチャンネル図は外部画像へ依存せず、テーマと画面幅へ追従するインラインSVGで描く。モノラルは1つの信号が左右へ分かれる関係、ステレオは異なる2つの信号が左右へ分かれる関係、5.1チャンネルはスクリーンに対する5個のスピーカーと低音用サブウーファーの位置関係を示す。色だけでなく、信号名、線種、配置でも区別する。
- `dr32`の計算問題は「周波数と周期」「ビット数と段階数」「音声データ量」のパターンごとに独立したスライドへ置き、そのパターン内から続けて出題する。問題オブジェクトの`level`は内部の分類として残してよいが、学習画面に「難易度」は表示しない。判定後は途中式を順番に示し、単位、2ⁿ、bitからBへの変換、1000倍・1024倍の換算などのポイントを問題ごとの解説へ含める。これらを別の「計算の確認」スライドへ重複掲載しない。
- `SoundQuiz`はページに存在する問題カテゴリだけを初期化し、`dr31`の波形問題と`dr32`の複数の演習スライドで共有する。`dr32`では各計算パターンが個別の状態を持ち、スコアは全スライドで共有する。同じ問題を複数回判定しても解答数・正解数を重複加算しない。
- 音声データ量の問題では、1000倍換算と1024倍換算を暗黙に切り替えず、各問題文へ換算規則を明記する。

### 計算と問題生成の規則

- `SoundCore`はDOMに依存させず、標本化、量子化、符号化、データ量、標本化定理の判定などの正解計算を担う。Controllerには入力状態だけを持たせ、派生値を重複保存しない。
- 基本の量子化範囲は0以上8未満とし、表現値を`最小値 + 段階値 × 量子化幅`で求める。最も近い表現値へ丸め、ちょうど中間なら上側を選び、範囲外は最小または最大の段階値へクリッピングする。
- 通常のランダム問題では量子化境界ちょうどの値を避ける。波形読み取り問題は正解となる格子上の標本値を先に決め、その点を通る滑らかな波形を描く。
- 問題の正解値は可能な限り`SoundCore`から生成し、宣言データへ同じ正解を重複して手入力しない。乱数は再現可能なシードを使い、同じ問題が連続しにくい状態を保つ。

### 画像のデジタル表現（`dr41`）

- `dr41.html`は、`/Users/takashi/授業スライド/情報_授業スライド_03デジタル.pptm`の53〜57枚目を基準に作成する。画像のデジタル化、解像度と階調、グレースケール、データ量の説明順を保つ。原本末尾の非表示スライドや、別単元の動画・プリンタ・圧縮へ範囲を広げない。
- 本文は「画像のデジタル化」「解像度と階調」「グレースケール」「画像のデータ量」「語句・ポイントまとめ」「符号化の問題」「データ量の問題」の7枚とし、共通スライド基盤を使う。語句はクリックで開く`details`、問題は入力と判定のUIを使う。
- 比較用の気球は`img/digital-representation/balloon.svg`に置いた独自の図であり、原本内の第三者画像を転載していない。1枚目は元画像 → 光の成分に分解 → 標本化 → 量子化 → 符号化の5図を横に並べ、「次へ」で1図ずつ追加表示する。初期値は32×32画素・各色2bit（4階調）。標本化の間隔（8〜64画素、8画素刻み）と階調（各色1〜8bit）はスライダーで変更し、表示済みの図を同じ条件で更新する。狭い画面では図の列だけを横スクロールし、工程を進めたときは新しい図を見える位置へ送る。
- 1枚目では各画素内のRGBの平均を計算する。中央の同じ4×4画素を枠で示し、標本化の平均値・量子化の段階値・符号化の2進数を拡大表で比較できる。まだ表示していない工程の値は拡大表にも先出ししない。標本化の平均値の表示だけを小数第1位へ丸め、量子化の計算は丸め前の値を使う。
- 2枚目で解像度と階調を改めて説明する。上段のなめらかな濃淡と下段の量子化後の濃淡を比べ、続いてRGBを合わせたカラー画像で効果を確認する。帯は明るさを白黒で示す独自SVGで、既存の`ImageCore.tone`を使って描く。各色の階調はスライダーで1〜8bit（2・4・8・16・32・64・128・256階調）に変え、帯・気球・色数・データ量を同じ条件で更新する。初期値とリセット後は各色2bit（4階調）。解像度は10・20・50・100・200・800の正方形から選び、階調を変えるときには解像度を保持する。JavaScript無効時も4階調の帯を読める。
- 階調の区切りは明るさ0以上256未満を等幅に分ける。8階調では0以上32未満を段階0、32以上64未満を段階1、…、224以上256未満を段階7とし、境界は上側へ含める。図の表示色は段階0を黒、最大段階を白とする。この規則と、角の明るさとマス内の平均の違いを補足dialogで説明する。
- グレースケールの4×4の段階値は原本56枚目の`6,5,4,3 / 5,4,3,2 / 4,3,2,1 / 3,2,1,0`を保持する。3桁の2進数を左上から右へ1行ずつ並べる。符号化問題は元画像・標本化・量子化の3図に続けて4×4の解答表を置き、図と同じ位置のセルへ入力する。入力中は量子化の図の対応する画素を枠で示す。狭い画面では入力表を3図の下へ配置する。問題の16画素と3枚目の図は同じ描画処理を共用し、3桁の0・1で判定する。全角数字は半角にそろえ、入力後は再判定できる。
- 計算問題は原本57枚目の2問を保持する。4096×3072・24bitフルカラーは各色256階調、1024倍換算で36MB。1000×800・32768（2の15乗）色は1画素15bit、1000倍換算で1.5MB。問題文へ換算基準を明記し、圧縮・付加情報を除く。解説は「次へ」で立式と換算を追加し、最後に約分・計算する。
- `js/image-core.js`はDOMに依存せず、RGB平均、量子化、符号化、データ量を計算する。`js/image-lessons.js`は各操作を初期化し、1つの図の読込失敗でほかの説明・問題を停止しない。見本画像は同一サイト内から読み込み、外部送信や生徒のファイル選択を必要としない。
- 初稿は`ページ一覧`に`release: false`で登録する。`back`はシリーズ目次の`dr00`とし、既存の音教材の前後関係や`dr00.next`を変更しない。`next`は2026-09-12の動画教材追加に伴い`dr42`とする。GASワークシートはこの初稿の作業範囲に含めない。
- 正解計算は`node scripts/test-image-core.mjs`で検証する。UI変更時はRGB3成分・全工程、解像度とビット数の全組み合わせ、原本の16画素と2問、未入力・誤答・再入力、解説の段階表示、共通スライド操作、狭い画面・テーマ・文字サイズを確認する。
- 2026年9月9日、Chrome・WebKitで1440px・760px・390px幅、全テーマ・3段階の文字サイズ、キーボード、タップ、補足dialogのEscapeとフォーカス復帰、全画面、JavaScript無効時を確認した。ページ内例外・ローカル資産の404は発生しなかった。タップはブラウザのタッチエミュレーションで確認し、Safariアプリ・スマートフォン実機は未確認。
- 5工程への変更後も同じ画面幅・テーマ・文字サイズで確認した。RGB3成分×標本化8通り×階調8通りの更新、工程の追加表示、符号の桁数、スライダーのドラッグ・キー・タッチ操作、狭い画面での新しい図への横スクロール、拡大表、解答表の4列すべての表示、入力と量子化図の対応を確認した。解答表は共通テーブルの最小幅480pxを継承せず、画面内へ4列を収める。
- 2026-09-12の濃淡図追加では、Chrome・WebKitで1〜8bitの全値について帯の段数・黒白の両端・キャプション・気球の条件・色数の連動、解像度と階調の独立操作、リセット、キー操作とマウスドラッグを確認した。390px・720px・1440pxでライト・ダークと3段階の文字サイズを確認し、横はみ出しはなかった。Chromeでは390pxのタッチエミュレーション、全画面、JavaScript無効時の上下の帯と4階調の初期表示も確認した。通常表示でページ内例外・ローカル404はなく、Safariアプリとタッチ端末実機は未確認。

### 動画のデジタル表現（`dr42`）

- `dr42.html`は、`/Users/takashi/授業スライド/情報_授業スライド_03デジタル.pptm`の59〜61枚目（64〜66枚目にも同じ内容）を基準にする。フレームとフレームレート、フレーム内・フレーム間圧縮、データ量と再生時間を扱う。続く伝送速度の問題とディスプレイ・プリンタはこのページに含めない。原本PPTMは変更しない。
- 本文は「動画とフレーム」「フレームレート」「動画の圧縮」「動画のデータ量」「語句・ポイントまとめ」「再生時間の問題」「データ量の問題」の7枚。共通スライド基盤を使い、語句は閉じた`details`、問題は数値入力・判定・段階表示の解説とする。
- ボールの往復運動と圧縮の模式図はHTML内の独自SVGで表す。最初は2秒の動きを6枚のフレーム（3fps）で見せ、各フレームの選択・前後移動・連続再生・一時停止を用意する。次のスライドでは1〜60fpsをスライダーで変え、固定30fpsと同時に比較する。両方に同じ経過時間を使い、fpsを変えても2秒で1往復する条件を保つ。
- 動画は自動再生しない。スライド離脱、タブ非表示、ページ離脱、動きを減らす設定への変更で停止する。画面の更新頻度や端末の処理で表示できる細かさに限界があることを添える。映画やテレビのフレームレートを一律の規格として断定しない。
- 圧縮は原本にある考え方の紹介にとどめ、特定の形式・コーデック・圧縮率を追加しない。フレーム内圧縮の濃淡図は細かな違いを省く例、フレーム間圧縮の破線はボールの移動前・移動後の両方を更新する範囲とする。移動前の位置を背景へ戻す点は補足dialogでも説明する。
- データ量の操作では24ビットフルカラー、1000倍換算、圧縮・音声・付加情報なしを固定し、解像度・fps・秒数を変える。1フレームの計算は既存の`ImageCore.imageSize`を共用し、`VideoCore`がフレーム数・動画全体・再生時間を求める。
- 問題は原本61枚目の条件を保持する。問題1は1フレーム1MB・24fps・全体1.5GB、1GB＝1024MBで64秒。問題2は800×600画素・24bit・30fps・1分、1000倍換算で1フレーム1.44MB、動画全体2592MB。問題1は原本の「情報関係基礎2021本試」の出典注記がある設問を、条件を保持して書き直したもの。換算条件を問題ごとに明記し、途中で丸めない。
- 初稿は`ページ一覧`に`release: false`・`contentType: lecture`で登録する。`dr41.next=dr42`、`dr42.back=dr41`とする。`dr42.next`は2026-09-12のディスプレイ・プリンタ教材追加に伴い`dr43`とする。ワークシートの作成・登録は今回の範囲に含めない。
- 正解と再生タイミングは`node scripts/test-video-core.mjs`、共通スライド構造は`node scripts/test-lesson-slide-pages.mjs`で検証する。ブラウザではフレーム選択、fpsを変えた同時再生、離脱時の停止、条件変更、数値判定、解説の段階表示、テーマ・文字サイズ・狭い画面・キーボードを確認する。
- 2026-09-12、Chrome・WebKitでフレーム操作、再生停止、fps比較、条件変更、原本2問の正誤・未入力・全角入力、解説の段階表示、補足dialogのEscapeとフォーカス復帰、全画面、JavaScript無効時を確認した。390px・720px・1440px、ライト・ダーク、標準・大・特大文字で横はみ出しはなく、ページ内例外・ローカル404も発生しなかった。タッチはブラウザのエミュレーションで確認し、Safariアプリとタッチ端末実機は未確認。管理表はセル値・書式・入力規則をAPIで照合したが、MacのロックによりGoogle Sheets画面の目視は未実施。

### ディスプレイとプリンタ（`dr43`）

- `dr43.html`は、`/Users/takashi/授業スライド/情報_授業スライド_03デジタル.pptm`の69〜73枚目を基準に作成する。70枚目のディスプレイ・発色・ppi、71枚目のリフレッシュレート、72枚目のプリンタ・dpi、73枚目の演習を保持する。04コンピュータの原本も確認したが、出力装置としての列挙だけなので、この教材の範囲は広げない。原本PPTMは変更しない。
- 本文は「ディスプレイの画素」「発色のしくみ」「リフレッシュレート」「プリンタとdpi」「印刷サイズと画素数」「語句・ポイントまとめ」「印刷の問題」の7枚。共通スライド基盤、閉じた`details`によるまとめ、数値入力と段階式の演習を使う。色の詳細はユーザーの方針どおり`dr22`の範囲とし、RGBサブピクセルとCMYKの紹介にとどめる。
- 画素図は1インチ四方のRGB配列を独自SVGのパターンで描き、8〜128ppi（8刻み、初期64）を変更する。原本の「64ppi、横幅20インチなら横1280画素」を同時に更新する。製品の対角線のインチ数とは区別し、ppiと画面全体の画素数の違いも短く説明する。模式図の表示サイズは実寸ではなく、RGB配列も一例とする。
- 発色はLCDとOLEDを並べ、明るさ0〜100%（10刻み、初期70%）で透過光と発光部を更新する。LCDのバックライトを一定に固定した基本モデルとし、製品固有の調光や細部構造は省略する。電圧の大小と透過率の方向を一律に断定しない。量子ドットの波長変換は補足dialogに置き、一般的なQLED液晶とOLEDの自発光を混同させない。JDI「ディスプレイの基礎」とSamsung「Quantum Dot display technology」で基本構造を照合した。図は第三者画像の転載ではない。
- リフレッシュレートは原本の60fps・30Hzを初期値とする。0.1秒間の動画30/60fpsと画面30/60/120Hzを比較し、「前の更新／次の更新」で画面が選ぶフレームを確認する。開始をt=0で同期し、更新時点の最新フレームを表示するモデルとする。60fps・30Hzは1→3→5、30fps・60Hzは1→1→2→2→3→3。動画の補間や実機のHz変更ではなく、連続自動再生も行わない。
- dpiは5dpiを固定基準とし、5/10/15/20dpi（初期10）の同じ1インチ四方を比較する。低い値は原本と同様の架空の値と明示する。原本の300dpiでは300×300＝90,000ドット、縦横2倍なら面積内のドット数4倍という関係を示す。
- 印刷計算は1インチ＝25.4mmを用い、横と縦を別々に換算する。条件操作は25.4mm四方・50.8mm四方・152.4×101.6mmと100〜600dpi（100刻み）を選び、初期値は25.4mm四方・300dpi。原本73枚目の演習は400dpi・横101.6mm×縦76.2mm・加工/拡大縮小/余白なし、答1600×1200画素を保持する。1画素を1ドットに対応させる計算上の条件を明示し、実際の印刷では複数のドットで1画素を表す場合があることを補足する（エプソンの解説で照合）。演習は横・縦別の判定、全角・正しい3桁区切りの数値入力、再入力・リセット、4段階の解説に対応する。
- 計算はDOM非依存の`js/output-core.js`、操作は`js/output-lessons.js`、図の配置は`css/output-representation.css`へ置く。共通スライド・ナビの処理は複製しない。JavaScript無効時も初期条件の図と本文、解答を読める。正解計算と更新フレーム列は`node scripts/test-output-core.mjs`で検証する。
- 2026-09-12、「配付ファイル一覧」GSSの`ページ一覧`へ`dr43`を16行目に挿入し、隣接する`dr42`の書式と入力規則を引き継いだ。`release: false`、`show: true`、`contentType: lecture`、`back: dr42`、`next: FALSE`とし、`dr42.next`だけを`FALSE`から`dr43`へ変更した。生成前に変更がこの2項目のみであることを公式プレビューで確認し、`exportData_to_pages_js`で再生成した（生成コミット`8035f85`）。登録前の復元点はサイトの`e69cbcb`と作業用`/private/tmp/dr43-gss-before.json`。登録を戻す場合は現行の行IDを照合したうえで今回の行と前後関係のみを戻し、正式生成する。既存の掲載状態・配付物・ワークシートは変更しない。
- 同日、Chrome・WebKitでppi全16値、明るさ0/100とリセット、fps2通り×Hz3通りの更新列、dpi4通り、印刷3サイズ×dpi6通り、問題の未入力・誤答・一方正解・全角・カンマ・再入力、解説4段階とリセットを確認した。1440px・720px・390px、テーマ自動/ライト/ダークと文字サイズ標準/大/特大で横はみ出しはなく、キーボード・390pxタッチエミュレーション・補足dialog2つのEscapeとフォーカス復帰・メニュー排他・全画面・JavaScript無効時の7枚と静的図・タイトル0枚目も確認済み。ページ内例外・ローカル404はなかった。Safariアプリ、タッチ端末実機、印刷プレビューは未確認。GSSの値・書式・入力規則はAPIで照合し、管理表の画面目視はMacのロックにより未実施。

### データの圧縮（`dr51`〜`dr52`）

- 2026-09-12のユーザー指定に従い、`dr51`で圧縮の概要・圧縮率・ランレングス法を1回、`dr52`でハフマン符号化を1回扱う。各ページは本文7枚に共通の表紙を加える。末尾はクリックで開く語句・ポイントまとめ、その後に問題演習を置く。ワークシートは今回の作成範囲に含めない。
- 原本は`/Users/takashi/授業スライド/情報_授業スライド_03デジタル.pptm`。74〜78枚目（80〜84枚目は重複）、85〜89枚目、91枚目の演習をXML・表セルと埋め込み資料で照合した。PPTMは変更しない。非表示のLZ法は授業範囲へ追加しない。
- RLEの文字列例は`AAAAAABBBBCCDAAA`の16文字を`A6B4C2D1A3`の10文字へ変換し、62.5%とする。白黒画像の例は5×5画素を行優先で読み、行末と次行先頭をつなぐ。`黒6白4黒4白1黒1白4黒1白4`の16文字で64%。例題・文字列演習では回数1も記録するが、原本78枚目の画像演習は同色3個以上のみ回数を記録し、1・2個は文字をそのまま残す。問題文で区別する。
- 画像演習は原本の表セルから自作SVGへ描き直す。図2は`黒6白3黒3白黒3`、図3で小さくならないものは②④、図4の符号長は①21・②19・③15文字で、③の圧縮率は60%。Web版の圧縮文字列は図そのものと明記された規則から算出し、原本の選択肢・正解数値を保持する。「黒」「白」「数字」を各1文字として比較する授業用モデルであり、UTF-8等の実ファイルサイズと混同しない。
- 文字列演習は`EEEECCABAB`の圧縮と`A8E1F1D4`の復元。前者は10文字から12文字へ増えるので、RLEで必ず小さくなるとは説明しない。圧縮率は一貫して「圧縮後÷圧縮前×100」とする。
- ハフマンの例は`DAEBCBACBBBCDAAABB`（A5・B7・C3・D2・E1）、固定3bitの54bitから38bitへ圧縮し、最後に小数第1位を四捨五入して約70%。原本の練習3例、問題1の20文字・34/40=85%、問題2の40文字・90/120=75%を保持する。空白は問題文の区切りで文字数に含めず、符号表等の付加情報も計算に含めない。
- 同頻度の結合順・0と1の枝の向きは一意でない。採点では原本の符号表から作った木と枝を問題の条件として与える。文字ごとの頻度が正しく入力された状態で判定すると、木にも頻度を表示する。別の割当が常に誤りであるとは説明しない。結合段階は原本の固定fixtureから復元し、各段階が残存する最少2重みを結合していることと、全体の最適bit数を独立計算で検証する。
- 概要の形式一覧は補足dialogへ置く。PNG・FLAC・ZIPと通常のJPEG・MP3・AACを代表例とし、MP4はデータを収めるコンテナであることを区別する。原本の「形式名だけで常に可逆性が決まる」「動画の差分が非可逆そのもの」といった一般化は引き継がない。PNGは[W3C仕様](https://www.w3.org/TR/png-3/)、FLACは[Xiph.Org](https://www.xiph.org/flac/index.html)、JPEGは[JPEG委員会](https://jpeg.org/jpeg/)、MP4は[Library of Congress](https://www.loc.gov/preservation/digital/formats/fdd/fdd000155.shtml)を確認した。
- 計算は`js/compression-core.js`、DOM操作は`js/compression-lessons.js`、図・入力部品は`css/compression-representation.css`で共用する。スライド移動、タブ、補足dialogは座学共通基盤を使い、ページ固有のナビゲーションは追加しない。図はHTML/SVGで、JavaScript無効時も問題・規則・解説を読めるようにする。
- `ページ一覧`の17〜18行へ`dr51`・`dr52`を追加し、隣接行の書式と入力規則（releaseのTRUE/FALSE、contentTypeのlecture/practice）を継承した。両方`release: false`・`show: true`・`contentType: lecture`、前後関係は`dr43→dr51→dr52`。正式プレビューで新規2ページと`dr43.next`以外に差分がないことを確認し、`exportData_to_pages_js`で再生成した（生成コミット`afd90cf`）。GSSの値・書式・入力規則はAPIで再読確認済み。MacがロックされているためGoogle Sheets画面での見た目は未確認。登録前の復元点は`e741f2c`と`/private/tmp/compression-gss-before.json`。一覧と検索には出さず、後日のユーザー確認で掲載を判断する。
- `node scripts/test-compression-core.mjs`で132件を検証する。2026-09-12、ChromeとPlaywright WebKitで両ページのRLE、全ハフマン例、符号化・復元、問題の正誤判定・リセット・段階解説、キーボード・タッチ・全画面・補足dialog・JavaScript無効時を確認した。`dr51`は1440・720・390pxと3テーマ・3文字サイズの代表組合せ、`dr52`は3幅×3テーマ×3文字サイズの27組合せで本文7枚すべてのページ横はみ出しを確認した。横長の木と表は領域内でスクロールする。表見出し・セルの配色はテーマ変数で明示する。検証コードと画像は作業用`/private/tmp/compression-qa/`、ページ内例外・404はなし。Safariアプリと端末実機のタッチは未確認。

### 掲載

- `index.html`には、明示的な掲載指示があるまでdrシリーズの入口を追加しない。
- ナビゲーション、検索対象、公開状態を変更するときは、`js/pages.js`を直接編集せず、「配付ファイル一覧」GSSの`ページ一覧`を正本として更新する。

## Network（nw）シリーズ

### 番号体系

| ID | 内容 |
| --- | --- |
| `nw11`〜 | インターネットとプロトコル、各階層 |
| `nw21`〜 | 暗号化 |
| `nw31`〜 | 情報システム |
| `nw41`〜 | データベース（将来対応） |

- 座学共通スライド基盤を使用し、ネットワーク教材固有の図と穴埋め操作は`css/network-mechanisms.css`と`js/network-lessons.js`へまとめる。
- 元スライドの機器、接続、情報の流れ、色分けが表す関係を保ちながら、図は拡大・テーマ変更へ対応できるHTMLとSVGで再構成する。原本の装飾をそのまま写すことより、図が示す関係の維持を優先する。
- 図中のオレンジ色の空欄は、クリック、タップ、Enter、Spaceで個別に答えの表示・非表示を切り替えるボタンとする。同じボタンを再操作すると、その語句だけを空欄に戻す。キーの長押しでは繰り返し切り替えず、キーボード操作中のフォーカスは操作したボタンに保つ。各スライドの表示数と一括表示・リセットの有効状態も同期する。JavaScript無効時と印刷時は答えを読める状態を保つ。
- `NetworkRevealGroup`は個別表示時に`joho:network-reveal-change`の`action: 'reveal'`、個別非表示時に`action: 'hide'`を送る。プロトコルの役割を隠すと、その役割の図も隠す。表示中の説明に対応する語句を隠した場合、または「すべて表示」の直後に一つ隠した場合は、説明を番号選択の案内へ戻す。他の表示済み語句・図は維持する。
- SVGの図と図中の穴埋めボタンは同じ固定座標面を基準に配置し、狭い画面では図の中だけを横スクロールさせる。説明の吹き出しと穴埋めボタンの領域を重ねない。
- 各スライドの一括表示とリセットは、最初の空欄より前に置く。

### インターネットとプロトコル（`nw11`）

- 原本は`/Users/takashi/授業スライド/情報_授業スライド_05ネットワークのしくみ.pptm`とし、通常表示されるスライド3、4、5、8を、インターネットのしくみ、データの伝送方式、プロトコル、プロトコルの階層構造の順で配置する。HTML独自の5枚目として「語句とポイントのまとめ」を加える。
- 1〜4枚目の冒頭には、前の内容と現在の内容の関係を説明する短い文章を置く。接続の全体像、データの伝送、共通の約束、階層での分担という因果関係が、口頭説明なしでも追えるようにする。
- 「インターネットのしくみ」の図は、PPT上のシェイプ座標を再現せず、接続関係を正本として描く。家庭側は、有線端末を重ならない配線でスイッチングハブへ、無線端末をアクセスポイントへ接続し、アクセスポイント、スイッチングハブ、ルーター、終端装置を独立した機器として示す。各機器の穴埋め語句と説明は対象機器の近くに置く。主回線は終端装置からISPを通ってインターネットへ接続し、通信キャリアもインターネットへ接続する。通信キャリアにはスマートフォンを無線接続する。「通信事業者」という分類ノードや、ISPと通信キャリアをまとめる分類線は置かない。会社側は、1台のルーター配下に総務LAN、営業LAN、サーバLANがそれぞれ存在する構成とし、ルーターへ「入口」など曖昧な補助ラベルを付けない。
- 「データの伝送方式」は、同じネットワーク図を方式①と方式②で切り替え、送るデータ、通信の準備、ネットワーク内を送る、回線の共有、受信と終了の5段階で比較する。方式を切り替えたときは「送るデータ」へ戻し、新しい方式を最初から確認できるようにする。方式①では確保した経路を点列が進み、方式②ではヘッダを持つA・Bの小包が異なる経路や共有回線を進み、到着順を確認して復元する様子を一度だけアニメーション表示する。「もう一度」で再生でき、動きを減らす設定では最終状態を直ちに示す。390px幅でも図自体を横スクロールさせない。
- 「プロトコル」は、原本の6個の穴埋め語句と説明順を保ち、端末間の1つの通信へ5つの役割を累積表示する。番号は処理順ではなく説明順として示す。各役割の穴埋めを選ぶと対応箇所を図へ追加し、短い説明を1つだけ表示する。パケットのヘッダは横長表ではなく送信データに付いた荷札として示し、送信先IP、送信元IP、通し番号、生存期間を390px幅でも横スクロールせず確認できるようにする。
- 「プロトコルの階層構造」は、送信側の第4層から第1層、伝送媒体、受信側の第1層から第4層までを9段階でたどる。4階層の一覧は画面幅にかかわらず第4層から第1層の縦1列とする。送信側ではTCP/UDP、IP、イーサネットの各ヘッダを順に付け、受信側では逆順に読んで外す。各段階で現在の階層、データの構造、短い説明を連動させ、390px幅でも横スクロールさせない。伝送媒体の0/1波形は二つの高さだけを使う模式図とし、完了時に階層化の利点を示す。
- 「語句とポイントのまとめ」は、4枚の内容を接続、伝送、約束、階層の順で結び直し、重要語句と押さえるポイントを同じスライドで確認できる構成とする。重要語句は語句ごとにクリック、タップ、Enter、Spaceで説明を開閉できるようにし、一括表示と一括閉じる操作も用意する。JavaScript無効時と印刷時は説明を読める状態を保つ。新しい学習範囲は追加せず、1〜4枚目で扱った語句と関係だけを整理する。
- 非表示スライドは通常の説明順へ含めない。補足として採用する場合は、授業範囲への影響を確認してから追加する。
- 次教材へのリンクは`ページ一覧`で管理し、`nw11`の次回を`nw12`へ、`nw12`の戻り先を`nw11`へ設定する。
- `nw41`以降は、対応する授業原本と扱う範囲が確定するまでページを作成しない。

### ネットワークインターフェース層（`nw12`）

- 原本は`/Users/takashi/授業スライド/情報_授業スライド_05ネットワークのしくみ.pptm`とし、通常表示されるスライド12〜16を参照する。HTMLでは、役割、有線、無線、MACアドレス、スイッチングハブ、語句とポイントのまとめ、問題演習の7枚に再構成する。
- 「物理層」「データリンク層」と、データ構造に関する「ヘッダ」「ペイロード」「フレーム」は高校の学習範囲外として扱う。図を簡潔にするために必要な場合だけ注釈へ置き、穴埋め、重要語句、問題演習の暗記対象には含めない。本文では「届けるための情報」「送りたいデータ」など、はたらきを表す言葉を優先する。
- 役割の図は、上の階層から受け取ったデータへ届ける情報を付け、0と1の並び、電気・光・電波の信号へ変える4段階を順に表示する。原本の階層分割を補足注釈として残しつつ、主説明はネットワークインターフェース層の一連のはたらきとして示す。
- 有線の図は、ツイストペアケーブルの電気信号と光ファイバーケーブルの光信号を並べて比較し、再生操作で信号の移動を示す。どちらも横方向に切り開いた構造として角の丸いカプセル形状を避け、ツイストペアケーブルは赤と青の2本が前後を入れ替えながら交差する二重らせんとして描く。ツイストペアケーブルには実物との対応を確認できるよう、DevcoreによるCC0の写真「RJ45.jpg」（Wikimedia Commons）を1280px幅に縮小して添える。
- 無線の図は壁や電子レンジを置かず、アクセスポイントと端末の接続関係へ絞る。2.4GHz帯と5GHz帯の特徴を切り替えて比較し、IEEE 802.11の具体的な規格表は「主な無線LAN規格」という補足の開閉項目へ置く。原本で「2014」となっているIEEE 802.11acの策定年は、規格名とIEEE公式の承認・発行年に合わせて「2013」に補正する。
- MACアドレスの図は、PC1からスイッチングハブを経てPC2へ送る接続と、送信元、送信先、送りたい内容の関係を示す。狭い画面では機器と配線を縦に並べ、配線と文字を重ねない。
- スイッチングハブの図は、PC1からPC2への送信と返信を6段階でたどる。送信元MACアドレスとポートの対応を学習し、未登録の送信先には複数ポートへ送り、返信後は記録を使って必要なポートだけへ送る流れを、移動するデータとMACアドレステーブルの変化で示す。
- 重要語句は1語句につき1つの開閉項目とし、初期状態ではすべて閉じる。問題演習は学習範囲内の媒体、Wi-Fi、MACアドレス、スイッチングハブの役割だけを扱い、解答後もレイアウトが急に変わらないよう解説欄の高さを確保する。

### インターネット層とIPアドレス（`nw13`）

- 原本は`/Users/takashi/授業スライド/情報_授業スライド_05ネットワークのしくみ.pptm`のスライド17〜23とする。通常表示される18、20、22、23を主な内容とし、HTMLでは、インターネット層の役割、IPv4アドレスの表し方、ネットワーク部とホスト部、IPアドレスの割り当てと変換、IPv4の不足とIPv6、語句とポイントのまとめ、問題演習の7枚に再構成する。
- 最初の図はnw12のMACアドレスからつなぎ、IPアドレスを「異なるネットワークをまたいで最終的な宛先を示す情報」として説明する。送るデータ、IPの追加、PC1からR1への送信、R1でMACを外す、R1からR2への送信、R2でMACを外す、R2からPC2への送信、PC2でMACを外す、上の層へ渡す、の9段階をSVGと情報欄で同期する。送信元・送信先IPはPC1・PC2のまま保ち、MACの組はPC1・R1、R1・R2、R2・PC2へ変わる。各区間を有線LANでつないだ模式図とし、R1とR2の間は1区間として描く。MACの機器名は各区間につながる接続口のアドレスを意味する。「ヘッダ」は補足へ置き、図が送信元・送信先の項目に絞ったものだと示す。「ペイロード」は使わない。NATによるIPアドレスの変換は4枚目へつなぐ。
- IPv4の表記は、32ビットの2進数、8ビットずつ4組、各組の10進数、ピリオドでつないだ表記の4段階で示す。ネットワーク部とホスト部は`192.168.100.1/24`と`192.168.100.1/26`を切り替え、サブネットマスクとCIDR表記の対応を色分けする。ネットワークアドレス、ブロードキャストアドレス、割り当て可能な範囲の計算はnw14へ送る。
- IPアドレスの割り当てと変換は家庭内LANを使い、プライベートアドレスとグローバルアドレスの区別、DHCPによる自動割り当て、NATによる送信時と受信時の変換を6段階で示す。原本の`.0`をルーターへ割り当てた例や、`123.345.1.xxx`、`202.162.0.aaa`など無効な表記は使用せず、プライベートアドレスと説明用の有効なグローバルアドレスを用いる。
- IPv4とIPv6は、32ビットと128ビットの長さ、表せるアドレス数を比較する。IPv4の不足、プライベートアドレスとNATによる有効利用、IPv6の利用を順につなぎ、現在はIPv4とIPv6が並行して使われていることを示す。
- 原本で非表示になっているスライド21のIPアドレスのクラス表はnw13へ含めない。nw14のIPアドレスの割り当てを扱う箇所で、歴史的な区分であることが分かる参考・補足としてのみ置き、重要語句や問題演習の暗記対象にはしない。
- 語句まとめは13語を1語句ずつ独立して開閉できるようにし、問題演習はインターネット層の役割、IPv4の表記、ネットワーク部とホスト部、プライベートアドレス、NAT、IPv6を扱う。IPアドレスのクラスとアドレス範囲の計算は出題しない。

## 保留中の判断

次の対象には保留事項がある。具体的な論点や修正内容がこの文書には残っていないため、変更依頼と直接関係する場合は作業前にユーザーへ確認する。

- `ss41.html`
- `html/answer1-4.html`
- `html/answer1-5_1.html`
